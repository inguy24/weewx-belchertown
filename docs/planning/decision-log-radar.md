# Decision Log — Radar Domain
Detailed round entries. See CLEAR-SKIES-PLAN.md for the one-line index.

Covers 3b rounds 14–16: keyless half (3b-14: RainViewer, IEM NEXRAD, NOAA MRMS, MSC GeoMet, DWD RADOLAN), keyed half sub-scope A2 (3b-15: OpenWeatherMap, Aeris, tile proxy), iframe config slot (3b-16). Also includes the ADR-015 amendment (2026-05-11: mapbox_jma dropped from day-1). Radar domain CLOSED at 3b-16.

---
- **2026-05-11** — Phase 2 task 3b round 14 closed. **Radar domain opened (keyless half)** per [ADR-015](../decisions/ADR-015-radar-map-tiles-strategy.md) + [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md). User picked Option A 2026-05-11: 5 keyless providers (`rainviewer`, `iem_nexrad`, `noaa_mrms`, `msc_geomet`, `dwd_radolan`) + `/radar/providers/{provider_id}/frames` endpoint this round; 3 keyed providers (`aeris`, `openweathermap`, `mapbox_jma`) + tile proxy endpoint deferred to 3b-15; `iframe` config slot deferred to 3b-16. Brief: [`phase-2-task-3b-14-radar-keyless-brief.md`](briefs/phase-2-task-3b-14-radar-keyless-brief.md) (373 lines — ~14% smaller than 3b-13's 429, addressing 3b-13 close lesson on brief weight). **Structurally novel:** radar has NO canonical-entity mapping (tiles are bytes, per canonical-data-model §4.5); CAPABILITY carries URL templates + content types; TWO module patterns (XYZ slippy for rainviewer + WMS-T for the 4 others); new shared helper `providers/_common/wms_capabilities.py` (defusedxml-based XML parser for WMS GetCapabilities TIME dimension, ~276 lines, MIT pure-Python dependency `defusedxml==0.7.1`); `ProviderCapability` dataclass extended with 4 optional radar fields (`tile_url_template` for XYZ; `wms_endpoint_url` + `wms_layer_name` for WMS-T; `tile_content_type` for both). New `RadarFrame` / `RadarFrameList` / `RadarFramesResponse` Pydantic models in `models/responses.py`. New `/radar` router + endpoint with path-param dispatch + 6-branch decision tree (404 for unknown / 404 for unconfigured / 200 happy-path / 502 transient / 503 quota / 502 protocol). New `[radar]` settings section. Teammate commits: api-dev landed impl in `fc43bf5` (5 provider modules + helpers + dataclass extension) + `fd3c508` (endpoint + dispatch + settings + wiring) + `9291dd5` (lead-status fallback); test-author landed `79b2dff` (5 fixture captures) + `8860ad5` (6 unit suites + 1 integration suite, 164 new tests) + `00511c6` (merge). **Lead-direct fixes consolidated in `f2362ee` (13 files, +298/-91)** covering nine items: (1) **brief rule-bug — rainviewer frame-kind**: brief said `past[i].time >= generated → current` but real RainViewer responses have all `past[].time` strictly BEFORE `generated` (latest past ~5 min before JSON creation); rule produced zero "current" frames. Correct: `max(past, key=time) → current`. Test-author's `test_canonical_translation_from_real_fixture` + `test_rainviewer_response_has_exactly_one_current_frame` caught it. (2-4) **3 WMS-T layer-name bugs**: `noaa_mrms.LAYER_NAME = "0"` → `"radar_base_reflectivity_time"`; `msc_geomet.LAYER_NAME = "RADAR_1KM_RDPR"` → `"RADAR_1KM_RRAI"` (RDPR doesn't exist in live capabilities — only RRAI/rain + RSNO/snow); `dwd_radolan.LAYER_NAME = "dwd:RX-Produkt"` → `"Niederschlagsradar"` (RX-Produkt absent in current GeoServer). All three would have failed at runtime against live WMS endpoints. Source: my api-docs files were written from upstream documentation rather than live GetCapabilities captures; values inherited into brief without re-verification. Test-author detected divergence at fixture capture and injected synthetic layers into XML fixtures to make tests pass against impl; surfaced via closeout B2-B4. (5-9) **Auditor F1-F5**: F1 HIGH (functional) — `_expand_period()` truncated from START of TIME range, producing decades/hours/days-stale "current" frames for IEM (2011-2026 range), NOAA (~6660 frames PT1S), DWD (~4 days PT5M); MSC unaffected (31 frames fit cap). Rewrote as end-anchored — walks backward from `end_iso`, emits last N timestamps, returns sorted ascending. Added 4 regression-guard integration tests (`TestF1RegressionGuardCurrentFrameNearEndOfPeriod`) asserting current frame == fixture end-of-period for each WMS-T provider. F2 MEDIUM (contract gap) — RainViewer per-frame `path` dropped from canonical layer; without it, dashboard couldn't construct tile URLs (CAPABILITY.tile_url_template has `{host}{path}` placeholders but wire shape was missing both). Added optional nullable `path` to `RadarFrame` + optional nullable `tileHost` to `RadarFrameList` (additive — non-breaking for existing clients). OpenAPI schemas extended in meta `cae579b`. F3 LOW — `_CACHE_TTL=60` deviates from ADR-017's 5-min default for radar metadata; documented deviation in all 5 provider modules; ADR-017 amendment deferred to a future round when radar TTL semantics are revisited holistically. F4 LOW — added 4 regression-guard tests for F1. F5 TRIVIAL — `dispatch.py` import order (radar imports were splitting the forecast group; reordered). **Pytest verified twice on weather-dev**: 1954/364/0 at `ecd7e75` (pre-round baseline matches 3b-13 close exactly); **2123/364/0 at `f2362ee`** (post-fix; +169 tests over baseline = 164 test-author + 5 lead F4 regression guards). **Lessons-triage (per CLAUDE.md "Capture lessons in the right place")**: one rule-shaped extension folded into [`rules/clearskies-process.md`](../../rules/clearskies-process.md) §"Cross-check canonical mapping cells against api-docs example responses at brief-draft" — new sub-rule "api-docs file provenance is part of the cross-check" requires "Captured: YYYY-MM-DD via <live URL>" provenance headers on api-docs files; files without are treated as suspect at brief-draft time. Why: the 3 WMS layer-name bugs all originated in api-docs files I wrote from upstream documentation without live capture. Four other observed lessons are decision-log-only (per user direction "default to decision-log; fold into existing rules where possible"): the rainviewer literal-vs-parenthetical confusion (one-off, not rule-shaped); test-author working on `weather-dev` directly rather than DILBERT (reasonable tooling workflow choice given fixture capture needs httpx in venv); `weather-dev` working tree drift via uid 1002 ownership of test files (tooling, not process); api-dev's `parse_wms_time_dimension` signature extension with `provider_id`/`domain` kwargs beyond brief (kwargs add error context, not a STOP violation). **Network caveat**: github.com:443 was again intermittently unreachable from DILBERT during the round; cleared mid-session several times. Per-side workarounds via fetch retries; surfaced via repeated `git fetch` failures. **Process notes carried into 3b-15 spawn**: the brief-bug class (rainviewer rule + 3 WMS layer names) shares one root cause — brief inherits wrong values from api-docs that weren't live-verified; new sub-rule above codifies the fix. Sync rule remains DILBERT-edit + weather-dev-run; if test-author works on weather-dev directly, surface to lead early so polling can target the right side. Meta repo commits: `22d4316` (brief + 5 api-docs opening) + `cae579b` (brief amendments + api-docs corrections + OpenAPI RadarFrame/RadarFrameList F2 extension) + plan-status close (this entry). API repo commits in order: `fc43bf5` `fd3c508` `9291dd5` `79b2dff` `8860ad5` `00511c6` `f2362ee` `61ea749`. **Phase 2 task 3b round 15 (radar keyed half — 3 providers + tile proxy endpoint per ADR-015 + ADR-037)** queued at `c:\tmp\3b-15-resume-prompt.md`.
- **2026-05-18** — Phase 2 task 3b round 15 closed. **Keyed radar half (sub-scope A2)** — openweathermap + aeris + tile proxy endpoint. Original scope was A1 (3 keyed + proxy); cut to A2 mid-research after discovering mapbox_jma raster-array architecture is incompatible with ADR-015's Leaflet decision; ADR-015 amendment landed in `2f49d4a`. Brief: [`phase-2-task-3b-15-radar-keyed-A2-brief.md`](briefs/phase-2-task-3b-15-radar-keyed-A2-brief.md). Impl: `providers/radar/openweathermap.py` (~360 lines) + `providers/radar/aeris.py` (~437 lines) + tile proxy endpoint + `ProviderHTTPClient.log_url` extension (LC-E Aeris path-credential redaction) + `wire_radar_settings()` + 2 dispatch rows. Tests: 169 per commit bodies (+160 pytest delta due to extension files); live-capture fixtures for both providers. Auditor (Sonnet, source-only): 0 blocking findings; 1 parking-lot note (radar endpoints missing `extra="forbid"` query-param model, consistent with 3b-14). api-dev closeout reconstructed from git (agent killed via TaskStop before submitting; impl commit `6903188` on origin/main). Pytest: 2283/364/0 at `ad1fe37` (lead-verified on weather-dev). API repo commits: `6903188` (api-dev) + `5465cda` `e0e85ca` `ad1fe37` (test-author). Meta repo commits: `2f49d4a` (ADR-015 amendment) + `e522033` (brief + api-docs) + plan-status close (this entry). **3b-16 next: iframe config slot (last radar round; closes radar domain + task 3b).**
- **2026-05-18** — Phase 2 task 3b round 16 closed. **Iframe config slot — closes radar domain + task 3b.** Operator-supplied iframe URL via CAPABILITY for regions without a tile-API path (BoM Australia, MetService NZ per ADR-015). No API calls, no tile bytes, no frames, no credentials — simplest provider module in the codebase. `make_capability(iframe_url)` factory replaces static CAPABILITY constant (frozen dataclass + runtime config). `iframe_url` on `ProviderCapability` dataclass (same extension pattern as 3b-14 radar fields). `/frames` returns 501 for iframe (not 404 — iframe IS a configured provider; 501 signals "operation not applicable to this provider type"). `/tiles` returns 404 via existing keyed-only check. Settings: `[radar] iframe_url` in api.conf (content, not a secret). Brief: [`phase-2-task-3b-16-radar-iframe-brief.md`](briefs/phase-2-task-3b-16-radar-iframe-brief.md) (133 lines). Auditor: 1 LOW finding (stale docstring — dispatch count "7" → "8" + missing iframe 501 branch in decision tree). Lead-direct fix in `ae4a86d`. Pytest: **2302/364/0** at `ae4a86d` (+19 above 2283 baseline). API repo commits: `f936ad3` (api-dev-1: module + capability + settings) + `c5f814e` (api-dev-2: wiring) + `e76ceff` (test-author: 19 tests) + `ae4a86d` (lead-direct F1 docstring fix). **Process evaluation note:** this round was the first test of the refactored process (terse rules, Sonnet auditor, focused agent prompts). See `c:\tmp\3b-16-process-evaluation.md` for token/efficiency comparison vs 3b-15 baseline. **RADAR DOMAIN CLOSED.** All 8 day-1 providers per ADR-015 implemented: rainviewer, iem_nexrad, noaa_mrms, msc_geomet, dwd_radolan (keyless, 3b-14) + openweathermap, aeris (keyed, 3b-15) + iframe (config slot, 3b-16). **PHASE 2 TASK 3b CLOSED.** 16 rounds total across 5 domains: forecast (5 providers), alerts (3), AQI (4), earthquakes (4), radar (8 incl iframe).
- **2026-05-11** — [ADR-015](../decisions/ADR-015-radar-map-tiles-strategy.md) amended (re-Accepted). `mapbox_jma` dropped from day-1 set — Mapbox JMA tilesets are raster-array shape (GL-JS-only, no band selection at the Raster Tiles API), incompatible with ADR-015's Leaflet decision. Day-1 list shrinks to 7 modules + iframe slot. Japan radar at v0.1 falls back to RainViewer. Surfaced during 3b-15 brief-draft research; first-class Japan support deferred pending alternative JMA feed or separate GL-JS ADR.
