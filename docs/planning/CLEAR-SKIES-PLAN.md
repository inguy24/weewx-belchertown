# CLEAR-SKIES-PLAN — A modern, modular weather UI for weewx

**Project name:** Clear Skies (working title; verified no conflicts in the weewx ecosystem 2026-04-29)

**Goal:** Build a modern, design-led weather website for `weather.shaneburkhardt.com` to replace the current Belchertown skin, structured as a small set of independently-installable, well-documented components that any weewx user can pick up and run on their own station.

**Status:** **Phase 2 tasks 1 + 2 + 3a-1 + 3a-2 + 3b rounds 1+2+3+4+5+6+7+8 complete (2026-05-10). 3b-forecast series + 3b-alerts series BOTH complete — all five day-1 forecast providers shipped per ADR-007, all three day-1 alerts providers shipped per ADR-016. 3b-8 added the third (final) alerts provider (OpenWeatherMap) and closed the alerts domain.** Phase 1 closed 2026-05-05 with 40 ADRs Accepted, OpenAPI contract, canonical-data-model spec, security baseline, and 5 public GitHub repos with Phase 1 CI scaffolding. Phase 2 task 1 (FastAPI scaffold) closed 2026-05-06 with 73 of 73 pytest cases green on `weather-dev`. Phase 2 task 2 (read-only DB user enforcement at startup per [ADR-012](../decisions/ADR-012-database-access-pattern.md)) closed 2026-05-06 with 151 / 143 / 136 pytest pass on `weather-dev` (MariaDB / SQLite / unit-only). Phase 2 task 3a-1 (data-heavy DB endpoints) closed 2026-05-06 with 342 / 349 pytest pass on `weather-dev`; contract expanded to 69 first-class Observation fields. Phase 2 task 3a-2 (meta + static + pure-compute endpoints — 8 paths) closed 2026-05-07 across 2 rounds with 129/22/0 MariaDB + 136/15/0 SQLite final. Phase 2 task 3b round 1 (alerts domain — NWS + shared `providers/_common/` infrastructure) closed 2026-05-07 with 642/24/0 across both DB and cache backends. Phase 2 task 3b round 2 (forecast domain — Open-Meteo as the day-1 forecast provider) closed 2026-05-07: 781/25/0 default + 156/25/0 integration + 3/0/0 Redis tier; auditor surfaced 4 findings all lead-direct remediated in `521f5e1`. Phase 2 task 3b round 3 (forecast NWS — second forecast provider, first end-to-end consumer of canonical `ForecastDiscussion` via NWS Area Forecast Discussion) closed 2026-05-08: 764/0/0 default + 177/27/0 MariaDB + 184/20/0 SQLite + 5/0/0 Redis on `weather-dev`; auditor surfaced 6 findings (F1 HIGH `ForecastDiscussion.headline`/`senderName` brief-vs-canonical drift caught by §4.1.4 NWS column → impl + tests fixed to honor canonical via new `_extract_afd_headline_and_sender` helper, F2 MED `if "404" in str(exc)` substring fragility → `ProviderError.status_code` attribute added and propagated through `ProviderHTTPClient`, F3-F6 LOW), all lead-direct remediated in `98ec7dc`; mid-round session-limit incident drove the new live-scratchpad rule, the new lead-direct-remediation-when-small rule, and 4 other process rules. Phase 2 task 3b round 4 (forecast Aeris — third forecast provider, first keyed provider on the project) closed 2026-05-08: 1056/29/0 default + 7/0/0 Redis on `weather-dev`; F13 redaction-filter `client_id` strip fired; auditor surfaced 7 findings (F1+F2 HIGH `except Exception` re-wrap dropped `retry_after_seconds` from `QuotaExhausted`, F3 MED capability-vs-runtime drift on `sunrise`/`sunset`/`windGustMax` user-accepted via Q2 widening, F4 MED `timestamp` brief-vs-impl, F5-F7 LOW), all lead-direct remediated in `e79948e` + test-fix followup `ab3294a`. Phase 2 task 3b round 5 (forecast OWM — fourth forecast provider, second keyed provider, applies L1 paid-tier-max-surface rule across the whole hourly+daily surface) closed 2026-05-09: 1206/32/0 default + 205/32/0 MariaDB integration + 212/25/0 SQLite integration + 10/0/0 Redis tier; auditor surfaced 4 LOW findings (F1 endpoint-level missing-appid → 502 coverage gap, F2 cache-bypass on basic-tier 401 path causing ~432K errors/day vs ~48 calls/day with cache, F3 narrative whitespace drift vs weatherText, F4 wire-shape int/float type-vs-emission nit), F1+F2+F3 lead-direct remediated in `769d589`/`4965957`/`baaf0cc`/`9db163b`, F4 deferred as documentation nit consistent with Aeris precedent. Phase 2 task 3b round 8 (alerts OWM — third + final day-1 alerts provider, closes alerts domain) closed 2026-05-10: 1489/39/0 default+integration + 17/0 Redis tier; auditor surfaced 1 LOW/NIT finding (F1 test module-docstring year claim "2026-04-30" while assertion correctly checks "2024-04-30" — epoch 1714485600 resolves to 2024-04-30 14:00 UTC), lead-direct remediated in `f2a180f`. Cross-check rule from 3b-7 close fired at brief-draft and surfaced zero canonical-vs-wire mismatches (verifying the rule works as intended). Q1=A graceful-empty-list on basic-tier 401 mirrored from 3b-5 forecast/owm precedent — same conflation, same operator-recovery action, same cache-parity TTL. Phase 2 task 3b rounds 9+ (AQI / earthquakes / radar) queued.

**Predecessor:** [archive/WEATHER-EVALUATION-PLAN.md](../archive/WEATHER-EVALUATION-PLAN.md). That plan completed its assessment work, then was superseded by this one when the project shifted from "evaluate alternative weewx skins" to "build a new modern stack from scratch." The drift-capture and MQTT-typo work it described are already complete.

---

## What this is, and what it isn't

**This project is:**
- A from-scratch modern weather UI: design-led, component-based, mobile-first, focus-driven (not data-cram).
- A small, modular, reusable set of building blocks any weewx user can install on their own station.
- GPL v3 licensed to mirror weewx itself.
- Documentation-first — each component is shippable to the community on day 1 of release.

**This project is not:**
- A fork of an existing weewx skin. Every weewx-ecosystem skin was rejected as visually amateurish (see [rules/weather-skin.md](../../rules/weather-skin.md)).
- Belchertown 2.0. Belchertown's tech model (Cheetah templates) is archaic; we're stepping outside it.
- A commercial product. No revenue ambition. Pure open-source contribution.

---

## Architecture overview

```
External provider APIs              weewx (existing)
(forecast, AQI, alerts,                 │
 earthquakes, radar)                    │ writes archive records to MariaDB
        │                               │ also publishes to weewx-mqtt → EMQX
        │ HTTPS                         │   (existing, for HA & power users)
        │                               │
        ▼                               ▼
┌────────────────────────────┐    ┌──────────────────────────┐
│  weewx-clearskies-api      │    │  weewx-clearskies-       │
│  (FastAPI/Py, sync)        │    │  realtime                │
│                            │    │  (Py, SSE bridge)        │
│  - reads MariaDB           │    │  weewx loop → SSE        │
│  - calls external APIs     │    │                          │
│    via PROVIDER PLUGIN     │    │                          │
│    MODULES internal        │    │                          │
│    to this repo (ADR-038)  │    │                          │
└──────────┬─────────────────┘    └──────────┬───────────────┘
           │  JSON over HTTPS                │  SSE over HTTPS
           ▼                                 ▼
        ┌──────────────────────────────────────────┐
        │   weewx-clearskies-dashboard             │
        │   (React 19 + Vite SPA — "Clear Skies")  │
        │   Tailwind v4 + shadcn/ui + Recharts +   │
        │   Lucide + Weather Icons                 │
        └──────────────────────────────────────────┘

      weewx-clearskies-stack (meta repo)
      ──────────────────────────────────
      docker-compose for the easy-button install,
      deploy guide, architecture diagrams,
      example HA configs (REST sensors + MQTT YAML)
```

**Clear Skies ships ZERO weewx extensions.** Every external data source — forecast, AQI, severe-weather alerts, earthquakes, radar — lives as a **plugin module inside clearskies-api** (per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md)). New providers are added by writing a new module in clearskies-api, not by shipping a separate weewx extension. This is load-bearing — do not propose, design, or document weewx extensions for any data source.

---

## Components (5 separable repos under `github.com/inguy24/`)

Repo names per [ADR-004](../decisions/ADR-004-repo-naming.md). 5-component breakdown rationale per [ADR-001](../decisions/ADR-001-component-breakdown.md). Tech-stack choices per [ADR-002](../decisions/ADR-002-tech-stack.md). Distribution per [ADR-039](../decisions/ADR-039-distribution-installation-mechanism.md).

| # | Repo | Responsibility | Distribution | Required? |
|---|------|---------------|--------------|-----------|
| 1 | **weewx-clearskies-api** | HTTP/JSON API serving (a) read-only access to weewx's archive DB, and (b) external provider data via per-provider **plugin modules internal to this repo** (per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md)). Python / FastAPI (sync) / SQLAlchemy 2.x. Versioned (`/api/v1/...`). | `pip install` + systemd unit, optional Docker image | Yes |
| 2 | **weewx-clearskies-realtime** | Small Python service that bridges weewx loop packets to Server-Sent Events. paho-mqtt is an optional install extra for the MQTT-subscriber mode per [ADR-005](../decisions/ADR-005-realtime-architecture.md). | `pip install` + systemd unit, optional Docker image | Yes |
| 3 | **weewx-clearskies-dashboard** | The SPA. React 19 + Vite + Tailwind v4 + shadcn/ui + Recharts + Lucide + Weather Icons. Config-driven (station name, units, palette, API URL). Built artifact is static HTML/CSS/JS. | Pre-built static bundle, git source, optional containerized `caddy + dist` | Yes |
| 4 | **weewx-clearskies-stack** | Meta repo: docker-compose for the easy-button install, deployment guide, architecture diagrams, example HA configs (`examples/home-assistant/sensors-rest.yaml` and `examples/home-assistant/sensors-mqtt.yaml`). | Just docs + compose file. The "front door" for new users. | Optional but recommended |
| 5 | **weewx-clearskies-design-tokens** | Tailwind config + named design variables (palette, spacing, typography), published as a standalone npm package so others can build their own dashboards using the same visual language. | npm package | **Deferred — Phase 6+.** Tokens still exist *inside* `weewx-clearskies-dashboard` from day 1; they just aren't extracted to a separate package until there's demand. |

### Coexistence with existing infrastructure

EMQX stays running on the `cloud` container (Home Assistant + power-user MQTT consumers keep working unchanged); `weewx-clearskies-realtime` is independent of it per [ADR-005](../decisions/ADR-005-realtime-architecture.md). New users get a working site without an MQTT broker — broker is only needed for HA integration. Operator weewx extensions that write custom archive columns flow through the column-mapping path at setup per [ADR-035](../decisions/ADR-035-user-driven-column-mapping.md). **Clear Skies ships zero weewx extensions** — every external data source is a `weewx-clearskies-api` plugin module per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md).

---

## Tech stack decisions

Locked per [ADR-002](../decisions/ADR-002-tech-stack.md). Summary below; ADR-002 is the authoritative source — when it changes, this summary follows. Do not amend this summary in place; update ADR-002 first.

| Component | Stack |
|---|---|
| API + realtime (Python) | FastAPI **sync** route handlers, SQLAlchemy 2.x **sync mode**, uvicorn behind reverse proxy. paho-mqtt as optional install extra for the realtime MQTT-subscriber mode per [ADR-005](../decisions/ADR-005-realtime-architecture.md). |
| Dashboard (JS) | React 19 + Vite, Tailwind CSS v4 (CSS-first config), shadcn/ui (copy-paste discipline), Recharts as primary chart lib, Lucide + Weather Icons. **Tremor dropped, ECharts dropped from primary** per ADR-002. |
| Real-time transport | Server-Sent Events (browser-side); MQTT optionally on the realtime-service input side per [ADR-005](../decisions/ADR-005-realtime-architecture.md). |
| TLS | Apache + certbot for native installs; Caddy (auto-LE) for the docker-compose path. |
| License | GPL v3 per [ADR-003](../decisions/ADR-003-license.md). |
| Distribution / installation | PyPI + container registries + GitHub Releases per [ADR-039](../decisions/ADR-039-distribution-installation-mechanism.md). |
| Internal dev/test environment | `weather-dev` LXD container on Ratbert (primary; per [rules/clearskies-process.md](../../rules/clearskies-process.md) "Dev/test runs in `weather-dev`"). DILBERT is editing-only. |

---

## Cross-cutting concerns (apply to every phase, every component)

### Security baseline (from day 1 — not bolted on later)

The security baseline is the union of decisions in:

- [ADR-008](../decisions/ADR-008-auth-model.md) — auth model (no end-user auth; optional `X-Clearskies-Proxy-Auth` shared secret for cross-host deploys).
- [ADR-012](../decisions/ADR-012-database-access-pattern.md) — read-only DB user enforced at the database AND a startup write-probe.
- [ADR-027](../decisions/ADR-027-config-and-setup-wizard.md) — secrets in `secrets.env` (mode 0600), env-var injection, configuration UI auth.
- [ADR-029](../decisions/ADR-029-logging-format-destinations.md) — structured JSON logs with auth/SQL-param-value redaction.
- [ADR-030](../decisions/ADR-030-health-check-readiness-probes.md) — health/ready on a separate loopback-bound port.
- [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md) — one-door reverse proxy; inner services bind to loopback (or trusted-LAN cross-host).
- [`rules/coding.md`](../../rules/coding.md) §1 — parameterized queries, input validation at trust boundaries, dangerous-function bans, IPv4/IPv6 dual-stack, dependency pinning.

**The per-component checklist is [`docs/contracts/security-baseline.md`](../contracts/security-baseline.md)** — the source of truth for security controls Phase 2+ work checks against. The list above is the reading list of source ADRs; the baseline doc consolidates them and adds cross-cutting controls (security headers, request size limits, rate limiting, systemd/Docker hardening flags, `pip-audit` / `npm audit` / `gitleaks` CI gates, `SECURITY.md` per repo).

### Documentation acceptance criteria (a phase isn't "done" without these)

Every component repo:
- `README.md` — one-paragraph description, screenshots, 5-line quick start, link to full docs.
- `INSTALL.md` — step-by-step for `pip`/Debian/Docker, including reverse proxy config and verification steps.
- `CONFIG.md` — every config option, defaults, examples.
- `DEVELOPMENT.md` — local dev setup, tests, contribution guide.
- `CHANGELOG.md` — release notes per tag.
- `LICENSE` — GPL v3.
- `SECURITY.md` — disclosure process, threat model.

API extras:
- Auto-generated OpenAPI spec served at `/docs`.
- `curl` example for every endpoint.
- Schema reference for the data shapes returned.

Dashboard extras:
- Theming/branding guide ("how to make it look like your station").
- Screenshot gallery.
- Component inventory: which components used and where they came from.

Stack repo extras:
- Architecture diagram (one good SVG).
- Multiple deployment topology examples (single-host, separate-host, Docker, bare-metal).
- Upgrade guide.
- Example HA configs (REST and MQTT variants).

### Versioning

- API wire contract: URL-path versioning (`/api/v1/...`), RFC 9457 errors, no support-window promise per [ADR-018](../decisions/ADR-018-api-versioning-policy.md).
- Per-repo release lifecycle: independent SemVer; pre-1.0 minor bumps may break per [ADR-032](../decisions/ADR-032-versioning-across-repos.md).
- OpenAPI spec ([`docs/contracts/openapi-v1.yaml`](../contracts/), Phase 1 deliverable) is authoritative; the dashboard code-generates a typed API client from it.

---

## Decision records

Architecture and process decisions are tracked as ADRs in [docs/decisions/](../decisions/). See the [Decision Index](../decisions/INDEX.md) for the full list. The plan references ADRs from its task tables — when a decision changes, update the relevant ADR (or supersede with a new one), not this plan body.

Project process discipline (when ADRs are written, format, lifecycle): [rules/clearskies-process.md](../../rules/clearskies-process.md).

---

## Phase plan

### Phase 1 — Architecture & contracts

Establish foundations that every later phase depends on. No production code yet — the deliverables are decisions, contracts, and scaffolds.

| Task | Status | Notes |
|------|--------|-------|
| Lock the 5-component breakdown | ✅ | [ADR-001](../decisions/ADR-001-component-breakdown.md) — Accepted |
| Tech-stack choices | ✅ | [ADR-002](../decisions/ADR-002-tech-stack.md) — Accepted |
| License = GPL v3 | ✅ | [ADR-003](../decisions/ADR-003-license.md) — Accepted |
| Repo naming finalized | ✅ | `weewx-clearskies-*` per [ADR-004](../decisions/ADR-004-repo-naming.md) — Accepted |
| Realtime architecture (direct + MQTT modes) | ✅ | [ADR-005](../decisions/ADR-005-realtime-architecture.md) — Accepted; MQTT shipped as optional install extra |
| Compliance model (end-user-managed keys) | ✅ | [ADR-006](../decisions/ADR-006-compliance-model.md) — Accepted |
| Forecast provider strategy | ✅ | [ADR-007](../decisions/ADR-007-forecast-providers.md) — Accepted. Research at [docs/reference/FORECAST-PROVIDER-RESEARCH.md](../reference/FORECAST-PROVIDER-RESEARCH.md); per-provider API docs at [docs/reference/api-docs/](../reference/api-docs/) |
| Auth model | ✅ | [ADR-008](../decisions/ADR-008-auth-model.md) — Accepted |
| Inbound traffic flow / one-door reverse-proxy architecture | ✅ | [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md) — Accepted |
| Configuration format, secret handling, configuration UI | ✅ | [ADR-027](../decisions/ADR-027-config-and-setup-wizard.md) — Accepted. ConfigObj/INI; on-demand standalone config UI (HTTPS by default with self-signed); admin user/password auth |
| Canonical internal data model | ✅ | [ADR-010](../decisions/ADR-010-canonical-data-model.md) — Accepted. 9 entity types + 2 containers; weewx-aligned camelCase JSON / snake_case Python; units = weewx target_unit with metadata block; single-station model; prose at three layers (weatherText, narrative, ForecastDiscussion). Companion full-catalog spec [`docs/contracts/canonical-data-model.md`](../contracts/canonical-data-model.md) committed 2026-05-05 — per-field enumeration, per-target_unit-system unit tables, provider→canonical mapping tables for the day-1 forecast/AQI/alerts/earthquake/radar providers. |
| Multi-station scope for v0.1 | ✅ | [ADR-011](../decisions/ADR-011-multi-station-scope.md) — Accepted. Single-station only; multi-station explicitly out-of-scope (revisit if demand surfaces); forward-compat path = non-breaking optional `stationId` field + `?station=` query param per ADR-010 |
| Design direction document | ✅ | [ADR-009](../decisions/ADR-009-design-direction.md) — Accepted 2026-05-02. Multi-page card-based dashboard with icon-rail nav, three-tier information hierarchy, operator-uploadable hero images with event-trigger system + shipped generic graphic default, Inter font with tabular figures, neutral-foundation palette + operator-picked accent + EPA AQI scale + semantic colors, all three theme modes (light/dark/auto-by-sunrise-sunset/auto-by-OS), restrained motion respecting `prefers-reduced-motion`, mobile-first non-negotiable, WCAG 2.1 AA throughout. Companion [ADR-024](../decisions/ADR-024-page-taxonomy.md) (page taxonomy) Accepted 2026-05-02 — 9 built-in pages (Now/Forecast/Charts/Almanac/Earthquakes/Records/Reports/About/Legal) with locked Lucide icons, custom-page mechanism, slim one-line footer with Legal link. Walk artifacts: [DESIGN-INSPIRATION-NOTES.md](../reference/DESIGN-INSPIRATION-NOTES.md), [BELCHERTOWN-CONTENT-INVENTORY.md](../reference/BELCHERTOWN-CONTENT-INVENTORY.md), [CLEAR-SKIES-CONTENT-DECISIONS.md](../reference/CLEAR-SKIES-CONTENT-DECISIONS.md). |
| Validate tech-stack choices via small spike | ✅ | Completed 2026-05-04 in `weather-dev:/home/ubuntu/spike/`. ADR-002 stack validated (Vite 8 + React 19 + TS 6 + Tailwind v4 + shadcn v4 + Recharts 3.8 + Lucide). Bundle 164.52 KB gzipped — under [ADR-033](../decisions/ADR-033-performance-budget.md)'s 200 KB budget by ~35 KB. Two scaffold-time footguns documented for the dashboard repo: `"overrides": { "react-is": "^19.2.0" }` in `package.json` before `npm install recharts`, and `"ignoreDeprecations": "6.0"` in `tsconfig.app.json` until TS7. Findings + re-run command: [docs/reference/SPIKE-FINDINGS.md](../reference/SPIKE-FINDINGS.md). First pass off-spec (used ECharts) per [PLAN-VS-ADR-AUDIT-2026-05-04.md](../reference/PLAN-VS-ADR-AUDIT-2026-05-04.md); second pass against the correct stack confirmed every ADR-002 choice. |
| Stand up `weather-dev` LXD container on ratbert | ✅ | Stood up 2026-05-04 at `192.168.2.113` (DHCP/SLAAC on `br-vlan2`). Ubuntu 24.04, `security.nesting=true` (Docker-in-LXC), 6 GB memory cap. Provisioned: Docker Engine 29.4 + Compose v5, Node 22 LTS, Python 3.12, uv, git, build-essential. Brought forward from the original Phase 4 entry — Windows host (DILBERT) is for editing only; all `docker compose`, `pytest`, `npm`, `vite`, etc. runs inside the container per [rules/clearskies-process.md](../../rules/clearskies-process.md) "Dev/test runs in `weather-dev`". Roster entry in [reference/ratbert-lxd.md](../../../Windows%20Server/reference/ratbert-lxd.md). |
| Build docker-compose dev/test stack | ✅ | Scaffolded 2026-05-04 at [`repos/weewx-clearskies-stack/dev/`](../../repos/weewx-clearskies-stack/dev/). MariaDB 10.11 service + Python seed loader. Snapshot capture script (`snapshot/capture.py`) is host-side, SQLAlchemy-reflection-based — whatever schema is live in production (stock + extension columns per [ADR-035](../decisions/ADR-035-user-driven-column-mapping.md)) flows through. Loader (`seed/seed_loader.py`) is backend-agnostic via a small generic-type map; same snapshot loads into MariaDB or SQLite per [ADR-012](../decisions/ADR-012-database-access-pattern.md). Compose profiles `mariadb` / `sqlite` / `all` select target backend; CI matrix runs both as parallel jobs. Validated end-to-end inside `weather-dev` 2026-05-04: both profiles built, MariaDB came up healthy, both seed runs loaded synthetic fixture and verified row counts. Validation surfaced and fixed two defects the prior Windows smoke test couldn't reach — invalid `pip install --require-hashes=false` Dockerfile syntax, and SQLite-volume permission collision with non-root container `USER`. |
| Write the API contract (OpenAPI spec) | ✅ | Committed 2026-05-05 at [`docs/contracts/openapi-v1.yaml`](../contracts/openapi-v1.yaml). OpenAPI 3.1, 23 paths, 53 schemas; validates clean against `openapi-spec-validator`. Endpoint inventory derived from [ADR-024](../decisions/ADR-024-page-taxonomy.md) page taxonomy + [ADR-010](../decisions/ADR-010-canonical-data-model.md) canonical entities. URL-path versioning + RFC 9457 errors per [ADR-018](../decisions/ADR-018-api-versioning-policy.md). Drove [ADR-040](../decisions/ADR-040-earthquake-providers.md) and an `EarthquakeRecord` addition to [ADR-010](../decisions/ADR-010-canonical-data-model.md). |
| Stand up the 5 GitHub repos | ✅ | Created 2026-05-05 under `github.com/inguy24/`. All five public, named per [ADR-004](../decisions/ADR-004-repo-naming.md), licensed GPL v3 per [ADR-003](../decisions/ADR-003-license.md): [api](https://github.com/inguy24/weewx-clearskies-api), [realtime](https://github.com/inguy24/weewx-clearskies-realtime), [dashboard](https://github.com/inguy24/weewx-clearskies-dashboard), [stack](https://github.com/inguy24/weewx-clearskies-stack) (already carries Phase 1 docker-compose dev content under `dev/`), [design-tokens](https://github.com/inguy24/weewx-clearskies-design-tokens) (Phase 1 name-reservation placeholder per ADR-001 — populated in Phase 6+). Each has `README.md` (placeholder), `LICENSE` (GPL v3), `SECURITY.md` (placeholder), language-appropriate `.gitignore`. Layout follows [ADR-036](../decisions/ADR-036-workspace-layout.md): each child is an independent git repo nested under `repos/`; meta `.gitignore` excludes `repos/*` to avoid double-tracking; [`weather-clearskies.code-workspace`](../../weather-clearskies.code-workspace) created with the meta folder + all five children. DILBERT→weather-dev sync is via [`scripts/sync-to-weather-dev.sh`](../../scripts/sync-to-weather-dev.sh) (manual fire after pushing). |
| Wire CI scaffolding | ✅ | Phase 1 minimal CI landed 2026-05-05 across all five repos. Three workflows per repo: `dco.yml` (verifies Signed-off-by trailer on every PR commit per [ADR-003](../decisions/ADR-003-license.md)), `gitleaks.yml` (secret scan on PR/push to main + nightly cron), `dep-audit.yml` (`pip-audit` for Python repos / `npm audit --audit-level=high` for JS repos / both for stack's `dev/` infrastructure; placeholder-tolerant — skips quietly until a manifest lands). Third-party actions pinned by SHA per [`coding.md`](../../rules/coding.md) §1: `actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5` (v4), `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065` (v5), `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020` (v4); gitleaks installed by tarball download (avoids the gitleaks-action license requirement on org repos). Lint/test/release-on-tag workflows + the [ADR-012](../decisions/ADR-012-database-access-pattern.md) DB matrix for api are deferred to Phase 2/3/4 when each repo has actual code to exercise. |
| Security baseline document | ✅ | Committed 2026-05-05 at [`docs/contracts/security-baseline.md`](../contracts/security-baseline.md). Per-component checklist consolidating [ADR-003](../decisions/ADR-003-license.md) + [ADR-008](../decisions/ADR-008-auth-model.md) + [ADR-012](../decisions/ADR-012-database-access-pattern.md) + [ADR-027](../decisions/ADR-027-config-and-setup-wizard.md) + [ADR-029](../decisions/ADR-029-logging-format-destinations.md) + [ADR-030](../decisions/ADR-030-health-check-readiness-probes.md) + [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md) + [`coding.md`](../../rules/coding.md) §1, plus cross-cutting controls (security headers, request limits, rate limit, systemd/Docker hardening, `pip-audit`/`npm audit`/`gitleaks` CI gates, `SECURITY.md` per repo). Four components covered (api / realtime / dashboard / stack); design-tokens deferred per ADR-001. §8 documents six known gaps & opinionated defaults — three with real teeth (multi-worker rate-limit storage, 1 MiB request body default, markdown sanitization regression risk) and three TODOs (realtime health port, DCO mechanism, dashboard CSP iteration). Self-audit pressure-tested ADR-030's split-port architecture; user confirmed keep-as-is. |
| Optimize older ADRs for conciseness | ✅ | Completed 2026-05-04. Nine bloated ADRs cut in place per item-7 lifecycle (ADR-008, ADR-009, ADR-010, ADR-011, ADR-024, ADR-027, ADR-036, ADR-037, ADR-038). Remaining 28 ADRs audited paragraph-by-paragraph for content density and confirmed tight — no filler, every paragraph load-bearing. Conciseness rule in [rules/clearskies-process.md](../../rules/clearskies-process.md) clarified: bloat is content density, not line count. Orphan `ADR-035-observation-entity-model.md` deleted. |

### Phase 2 — clearskies-api MVP

The data layer. Boring, secure, fast.

**Multi-agent execution:** Phase 2 onward uses Claude Code Agent Teams (experimental, gated by `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`). Lead = Opus (this session). Sonnet teammates implement; Opus `clearskies-auditor` teammate reviews all output before lead synthesis. Custom agent definitions in [.claude/agents/](../../.claude/agents/). Recommended team size 3–5; auditor counts toward the limit.

| Task | Status | Teammate(s) | Notes |
|------|--------|-------------|-------|
| FastAPI scaffold | ✅ | `api-dev` | Closed 2026-05-06. 8 commits on github.com/inguy24/weewx-clearskies-api `main`: initial scaffold (39 files, 3095 insertions) + 3 fix commits surfaced by pytest in `weather-dev` (regex group reference in redaction filter, missing path-existence check in `load_settings`, Authorization regex stopping at first whitespace) + `uv.lock` + 2 dep-audit workflow fixes (Phase 1 workflow audited the runner env instead of project deps; latent bug surfaced when this repo got real Python code) + a fastapi 0.115.12→0.136.1 / starlette 0.46.2→1.0.0 bump that cleared two real CVEs (CVE-2025-54121, CVE-2025-62727). 73/73 pytest pass; both CI workflows (`gitleaks`, `dep-audit`) green. Implements security-baseline §3.1 (network listener, dual-stack, body limit, rate limit, CORS, security headers), §3.2 (proxy-auth shared secret, four-case [ADR-008](../decisions/ADR-008-auth-model.md) matrix), §3.4 (config secrets via env-var + secret-leak guard at startup), §3.6 (JSON logging + redaction filter incl. SQL parameter values), §3.7 (separate-port health on loopback). §3.3 (DB) deferred to task 2; §3.5 (full input validation) deferred to task 3 when real Pydantic models land; §3.8 (process hardening) deferred to task 7. Multi-agent execution: api-dev (Sonnet) implementation, auditor (Opus) reviewed across 9 findings + a diff-only re-audit, lead (Opus) synthesis. |
| Read-only DB user enforcement at startup | ✅ | `api-dev` + `test-author` parallel; `auditor` review | Closed 2026-05-06. SQLAlchemy 2.x engine, INSERT-into-archive write-probe with fail-closed exception taxonomy (`IntegrityError`/`OperationalError` 1142/1143/SQLite-readonly = denied; everything else exits critical), `MetaData.reflect()` + `ColumnRegistry` (86-entry stock-column lookup from [`canonical-data-model.md`](../contracts/canonical-data-model.md) §3), per-request session via FastAPI DI, `SELECT 1` readiness probe wired through `register_readiness_probe()`. Reflection failure now fatal at startup (matches write-probe pattern; restart loop is the retry mechanism). DB host validation handles IPv4/IPv6/hostname cleanly with a host:port-typo guard. Both backends green via `dev/` docker-compose stack: 151/143/136 pytest on `weather-dev` (MariaDB / SQLite / unit-only). 27 integration tests selectable via `pytest -m integration`. Stack repo gained `dev/mariadb-init/01-clearskies-ro.sql` to provision the `clearskies_ro` SELECT-only user idempotently. Multi-agent execution: api-dev (Sonnet) implementation across three rounds (initial → 2 bug fixes from integration tests → 5 auditor findings), test-author (Sonnet) parallel integration-test authoring, auditor (Opus) review. Process win: integration tests caught 2 real defects unit tests missed (writable user passing the probe via `IntegrityError`; SQLite URL form failing `MetaData.reflect()`); auditor caught 5 source-side findings (integration marker dead, INSTALL.md ref, pool config gap, reflection-non-fatal vs `/health/ready` disagreement, IP heuristic). |
| Endpoints sub-round 3a-1 — data-heavy DB endpoints (6 paths: `/current`, `/archive`, `/records`, `/reports`, `/reports/{year}/{month}`, `/reports/{year}`) | ✅ | `api-dev` + `test-author` parallel; `auditor` review | Closed 2026-05-06. Brief: [`docs/planning/briefs/phase-2-task-3a-1-brief.md`](briefs/phase-2-task-3a-1-brief.md). Final state: 342 / 349 pytest pass on `weather-dev` (MariaDB / SQLite), 0 failures on either. 84 integration tests selectable via `pytest -m integration`. Implements security-baseline §3.5 (input validation via Pydantic + `extra="forbid"` wrapped through a custom `Depends()` pattern that actually enforces the gate). Contract expansion: OpenAPI `Observation` schema grew from 22 to 69 first-class numeric fields per user direction "full Belchertown replacement, no hidden weewx capabilities" — eliminates the "promotion candidates routed through `extras`" framing; `extras` is operator-custom-only. Canonical-data-model §3.1 rewritten to mirror `STOCK_COLUMN_MAP`. New OpenAPI path `/reports/{year}` + `NOAAYearlyReport` / `YearlyReportResponse` schemas added (Phase 1 contract had monthly NOAA only — gap surfaced during brief research; weewx writes both). New units helper module `services/units.py` reads weewx.conf at startup (configobj) for operator-override support per [ADR-019](../decisions/ADR-019-units-handling.md) §Decision; new `[weewx] config_path` config knob. Aggregation modes `raw`/`hour`/`day` all working; `hour` uses dialect-aware bucket helper for SQLite (`strftime`) vs MariaDB (`FROM_UNIXTIME`); `day` reads weewx's `archive_day_<obs>` summary tables with per-canonical-field aggregator map (`DAY_AGGREGATOR`). Cumulative-vs-delta semantics nailed for `daySunshineDur` (max, not sum). Records section assignment is lead+user-confirmed in the brief and baked as a constant in `services/records.py`. Multi-agent execution: 4 remediation rounds — round 1 (test-author caught 3 real bugs api-dev's pre-submit pytest missed: MariaDB reserved-word `interval` partial fix, `extra="forbid"` not firing through individual `Query()` decls, `OperationalError` masking on empty archive), round 2 (auditor source review: 6 findings — Observation expansion, narrow-Exception catches, dead code, sort-precedence test gap, regression test for stock routing), round 3 (`daySunshineDur` aggregator flip), round 4 (`_GROUP_MEMBERS` 3 misclassifications: `snow` missing from group_rain, `snowRate` in group_rain not group_rainrate, `sunshineDur` missing from group_deltatime). Process wins: dual-backend integration tests caught dialect drift the unit suite missed; auditor's source review caught structural issues the runtime didn't reach; the parallel-pull sequencing rule from task 2 close was reaffirmed (api-dev round 1 missed 3 bugs by running pytest before test-author's commits landed). Lessons captured in `rules/coding.md` §1 (Pydantic + `Depends` pattern; SQL reserved-word backticking), `reference/weather-skin.md` (day*-prefix cumulative semantics), `rules/clearskies-process.md` (no promotion-candidates framing), `.claude/agents/clearskies-api-dev.md` (parallel-pull-then-pytest gate). |
| Endpoints sub-round 3a-2 — meta + static + pure-compute (8 paths: `/almanac`, `/almanac/sun-times`, `/almanac/moon-phases`, `/station`, `/capabilities`, `/pages`, `/charts/groups`, `/content/about`, `/content/legal`) | ✅ | `api-dev` + `test-author` parallel; `auditor` review | Closed 2026-05-07. Briefs: [`phase-2-task-3a-2-brief.md`](briefs/phase-2-task-3a-2-brief.md) + [`phase-2-task-3a-2-remediation-1-brief.md`](briefs/phase-2-task-3a-2-remediation-1-brief.md). Final state: 129/22/0 MariaDB + 136/15/0 SQLite integration on `weather-dev`, 0 failures either backend. New deps added: `skyfield` (lazy-downloads DE421 ephemeris ~17 MB on first run to operator-configurable cache, default `/var/cache/weewx-clearskies/skyfield/`). New api.conf sections: `[station] station_id` (defaults to slugified weewx.conf `[Station] location`), `[station] timezone` (3-tier fallback: api.conf → weewx.conf → OS TZ → UTC + WARN), `[content] directory` (default `/etc/weewx-clearskies/content/`, expects `about.md` + `legal.md`), `[pages] hidden` (operator-hideable built-in slugs; can't hide `now` per ADR-024), `[almanac] ephemeris_directory`. New shared service module `services/weewx_conf.py` extracted from `services/units.py` (single ConfigObj parse cached at startup, consumed by both units + station loaders). Round 1 implementation + tests + 1 SQLite-pool fix; round 2 was 4-finding remediation: F1 configobj-comma list-parse helper applied to location + station_type + timezone (not just altitude); F2 day-window switched from UTC midnight to station-local midnight in compute_almanac + daylightDeltaVsYesterdayMinutes + sun-times year loop + moon-phases month/year loops; F3 nine bare `except Exception:` blocks in almanac.py removed (Loader paths narrowed to `(OSError, IOError, urllib.error.URLError, ValueError)`; per-day compute paths drop the catch entirely so polar-edge handling is `if events.size == 0:` not exception-driven, and real bugs propagate to FastAPI's RFC 9457 handler); F4 `/station` "no such table" speculative branch dropped (production unreachable per ADR-012's startup fail-closed). OpenAPI line 1207 altitude description corrected to defer to units block per ADR-019 (lead-owned doc fix in same close commit). Multi-agent execution lessons captured in [rules/clearskies-process.md](../../rules/clearskies-process.md) — three sub-agents hit the agent-teams post-completion idle bug; filed [anthropics/claude-code #56930](https://github.com/anthropics/claude-code/issues/56930) with timing evidence; new lead-side discipline = `SendMessage` poll teammates every ~4 min, don't trust silence-as-still-working. |
| Endpoints sub-round 3b round 1 — alerts domain (NWS + shared provider infrastructure) | ✅ | `api-dev` + `test-author` parallel; `auditor` review | Closed 2026-05-07. Briefs: [`phase-2-task-3b-1-alerts-brief.md`](briefs/phase-2-task-3b-1-alerts-brief.md) + [`phase-2-task-3b-1-remediation-1-brief.md`](briefs/phase-2-task-3b-1-remediation-1-brief.md). Final state: 642 / 24 / 0 pytest pass on `weather-dev` across both DB backends (MariaDB / SQLite) AND both cache backends (memory + Redis via `fakeredis`). Implements ADR-038 §3 shared provider infrastructure (`providers/_common/`: `errors.py` canonical taxonomy — `QuotaExhausted`/`KeyInvalid`/`GeographicallyUnsupported`/`FieldUnsupported`/`TransientNetworkError`/`ProviderProtocolError`; `http.py` sync httpx wrapper with retry/backoff; `cache.py` pluggable backend per [ADR-017](../decisions/ADR-017-provider-response-caching.md) — both `MemoryCache` default + `RedisCache` activated by `CLEARSKIES_CACHE_URL`; `rate_limiter.py` sliding-window primitive; `capability.py` registry plumbing; `dispatch.py` explicit `PROVIDER_MODULES` dict per ADR-038 §Internal contract). NWS alerts module at `providers/alerts/nws.py` (5 module responsibilities per ADR-038 §2: outbound API call to `/alerts/active?point=<lat>,<lon>`; wire-shape Pydantic validation against recorded fixture; canonical normalization to `AlertRecord` per [canonical-data-model.md §4.3](../contracts/canonical-data-model.md); CAPABILITY symbol; canonical-error translation). `/alerts` endpoint with `severity` filter + no-provider fallback (`source="none"` per ADR-016 §Operators outside covered regions); `/capabilities` extended to populate `providers` list (3a-2 left empty); ProviderError → RFC 9457 ProviderProblem at `errors.py` (502 / 503 mapping per [ADR-018](../decisions/ADR-018-api-versioning-policy.md) + OpenAPI ProviderProblem schema). New runtime deps: `httpx` (sync mode), `redis` (the redis-py package); new dev-extras: `respx`, `fakeredis`. Dev/test stack at `repos/weewx-clearskies-stack/dev/` gained a `redis:7-alpine` profile. Round 1 implementation + tests + 1 lead-patch (test fixture state-bleed: `scope="module"` → function on integration app/client fixtures); round 2 was 5-finding auditor remediation: F1a `endpoints/alerts.py` broad-Exception → `pydantic.ValidationError`; F1b `RedisCache.__init__` ping broad-Exception → `redis.exceptions.RedisError`; F2 `RedisCache.get/set` mid-flight raw-redis-exception leak fixed by wrapping in `try/except RedisError` with WARNING-log + degraded behavior; F12 `nws.fetch()` return type flipped back from `list[dict]` to `list[AlertRecord]` (round-1 capitulation to tests reversed; cache stores dicts via `model_dump()`, cache-hit reconstructs via `model_validate()`); F17 wire-shape `_NwsAlertFeature.type`/`_NwsAlertsActiveResponse.type` tightened from `str` to `Literal["Feature"]`/`Literal["FeatureCollection"]`. F13 (redaction filter `client_id` strip) deferred to 3b round 2 (Aeris); F15 (fixture teardown symmetry) lead-owned and pushed in `e056ba8`. Polling discipline carry-forward from 3a-2 worked correctly this round: api-dev hit the post-completion idle bug on round 1 (TaskStop'd at lead-side after impl committed, closeout reconstructed from git log + commit messages); test-author hit it on remediation (TaskStop'd; closeout reconstructed from `321ec6b`'s diff). Auditor (Opus, last action = closeout) submitted cleanly without idle gap, matching the pattern documented in 3a-2's lessons. |
| Endpoints sub-round 3b round 2 — forecast domain (Open-Meteo as day-1 forecast provider) | ✅ | `api-dev` + `test-author` parallel; `auditor` review; lead-direct remediation | Closed 2026-05-07. Brief: [`phase-2-task-3b-2-forecast-brief.md`](briefs/phase-2-task-3b-2-forecast-brief.md). Final state: 781/25/0 default + 156/25/0 integration + 3/0/0 Redis tier on `weather-dev`. Implements `providers/forecast/openmeteo.py` (~714 LOC; ADR-038 five-responsibility pattern; column-oriented wire-shape Pydantic models; per-target-unit query-param mapping per ADR-019; station-local→UTC time conversion via `utc_offset_seconds` per ADR-020; WMO weather-code → `weatherText` and `precipType` lookup tables; cache stores `ForecastBundle.model_dump(mode="json")`, reconstructs via `model_validate()` per F12 pattern; rate limiter "be polite" guard 5/sec). New `/forecast` endpoint with `hours`/`days` slice params (defaults 48/7; bounds 0-384/0-16); `ForecastQueryParams` via `Depends(_get_forecast_params)` per security-baseline §3.5. New `[forecast] provider = openmeteo` api.conf section + `ForecastSettings` dataclass mirroring `AlertsSettings`. New dispatch row `("forecast", "openmeteo")` in `providers/_common/dispatch.py`. `_wire_providers_from_config()` extended for the forecast domain (single-source-per-domain per ADR-007). Pydantic response models added to `models/responses.py`: `HourlyForecastPoint`, `DailyForecastPoint`, `ForecastDiscussion`, `ForecastBundle`, `ForecastResponse`. Open-Meteo is keyless so F13 redaction-filter `client_id` strip stayed deferred. **Lead-direct artifacts:** `ab017f1` fixed 2 pre-existing archive integration tests (`test_archive_default_without_params_returns_seed_rows` renamed to `test_archive_with_seed_window_returns_seed_rows`; `test_archive_cursor_pagination_yields_all_rows_exactly_once` updated) by passing explicit `from`/`to` covering the seed window — the failures were pre-existing on `321ec6b` (3b-1 closeout) per archive-verify investigation, root cause was seed timestamps drifting outside the endpoint's default 24h window over time. `521f5e1` lead-direct remediated all 4 audit findings: F1 (`_common/http.py` 4xx generic-translation block now logs `body_excerpt` at ERROR before raising AND includes a 200-char body excerpt in the `ProviderProtocolError` message — Open-Meteo's `{"error": true, "reason": "..."}` body now surfaces in operator logs and the ProviderProblem `detail` field; symmetric with the existing 5xx WARNING log; future keyed-provider rounds must audit whether 4xx bodies could echo back auth credentials), F2 (impl + tests left as-is using canonical-data-model §3.3's `"freezing-rain"` enum value for WMO 56/57/66/67; brief lead-resolved-call 12 amended post-hoc to specify `"freezing-rain"` for those codes; canonical-data-model §4.1.2 added a forecast-domain rule locking the canonical-enum-literal discipline for all future provider rounds; new lesson added to [.claude/agents/clearskies-api-dev.md](../../.claude/agents/clearskies-api-dev.md) about brief-vs-impl-vs-canonical divergence requiring STOP-and-ping even when impl matches the contract), F3 (`tests/test_providers_forecast_unit.py:42-45` `import time` + `from unittest.mock import patch` removed — `rules/coding.md` §3 dead-code), F4 (`_now_utc_iso8601` definition + call site in `openmeteo.py` replaced with `utc_isoformat(datetime.now(tz=UTC))` from existing `models/responses.py`; brief explicitly required reuse). Cache backends: both verified green by `redis-run` agent (3/0/0 Redis-tier integration tests against real `redis:7-alpine`). |
| Endpoints sub-round 3b rounds 3-5 — forecast (NWS / Aeris / OWM / Wunderground, one per round) + AQI + earthquakes + radar | ⬜ | `api-dev` + `test-author` per round | One round per remaining provider domain or per remaining forecast provider. Each domain is a `weewx_clearskies_api/providers/{domain}/` plugin module per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md) consuming the shared infrastructure landed in round 1. Whichever round first ships a `client_id`-using provider (Aeris forecast or alerts) extends `logging/redaction_filter.py` per F13 deferral. The next forecast round (3b round 3) is likely NWS — same wire-shape ergonomics as 3b-1's NWS alerts module already covered, plus the canonical `ForecastDiscussion` shape exercised end-to-end via NWS AFD. |
| SQLite + MariaDB support behind one config knob | ⬜ | `api-dev` + `test-author` | Default to whichever the local weewx uses; CI runs both backends per [ADR-012](../decisions/ADR-012-database-access-pattern.md) |
| Auto-generated OpenAPI spec at `/api/v1/docs` | ⬜ | `api-dev` | FastAPI does this for free |
| Test suite with realistic mock data | ⬜ | `test-author` (parallel with `api-dev`) | Unit + integration; integration runs against the docker-compose dev/test stack |
| systemd unit + Docker image | ⬜ | `api-dev` | Both shipped |
| Documentation (`README`, `INSTALL`, `CONFIG`, `SECURITY`, `DEVELOPMENT`) | ⬜ | `docs-author` | Acceptance gate |
| Tagged release v0.1 | ⬜ | lead (Opus) | First public artifact |

Dev environment: docker-compose dev/test stack from Phase 1 (`clearskies-stack/dev/`) — real MariaDB seeded with production weewx archive data. Same backend as production; defense in depth via `SELECT`-only grant per [ADR-012](../decisions/ADR-012-database-access-pattern.md).

### Phase 3 — clearskies-dashboard MVP

The visible artifact. Where design discipline gets exercised. Multi-agent execution per Phase 2 prelude — `dashboard-dev` (Sonnet) implements, `test-author` (Sonnet) writes Playwright + axe-core suites in parallel, `auditor` (Opus) reviews against design + a11y ADRs.

| Task | Status | Teammate(s) | Notes |
|------|--------|-------------|-------|
| Scaffold from chosen starter template | ⬜ | `dashboard-dev` | Vite + React 19 + shadcn/ui dashboard template per [ADR-002](../decisions/ADR-002-tech-stack.md); clone, strip to skeleton, replace with our design. Apply the two scaffold-time footguns from [SPIKE-FINDINGS.md](../reference/SPIKE-FINDINGS.md) (`react-is` override + `ignoreDeprecations` for tsconfig). |
| Wire to **mock data first** | ⬜ | `dashboard-dev` | UI develops independently of weewx; fast feedback loop |
| Implement priority pages (Now, Forecast, Charts) | ⬜ | `dashboard-dev` (one task per page; lead may run 2–3 page-builder teammates in parallel rounds) | Pages are mostly independent per [ADR-024](../decisions/ADR-024-page-taxonomy.md) — strong fit for parallel teammates |
| Implement remaining built-in pages (Almanac, Earthquakes, Records, Reports, About, Legal) | ⬜ | `dashboard-dev` (parallel rounds) | 9 built-in pages total per [ADR-024](../decisions/ADR-024-page-taxonomy.md); operator-hideable |
| Wire to real `clearskies-api` | ⬜ | `dashboard-dev` (coordinates with `api-dev` if contract questions surface) | Replace the mock with the typed API client generated from `docs/contracts/openapi-v1.yaml` |
| Mobile-first verification | ⬜ | `dashboard-dev` + `auditor` | Real device + Chrome DevTools |
| Light/dark mode + auto-by-sunrise/sunset + auto-by-OS | ⬜ | `dashboard-dev` | Per [ADR-023](../decisions/ADR-023-light-dark-mode-mechanism.md) |
| Theming/branding config | ⬜ | `dashboard-dev` | CSS variables + curated accent palette + operator logo upload per [ADR-022](../decisions/ADR-022-theming-branding-mechanism.md) |
| Documentation | ⬜ | `docs-author` | Including the theming guide and screenshot gallery |
| Tagged release v0.1 | ⬜ | lead (Opus) | Second public artifact |

### Phase 4 — clearskies-realtime + integration test environment

Multi-agent for the SSE bridge work; deploy rehearsal is single-track (lead-driven).

| Task | Status | Teammate(s) | Notes |
|------|--------|-------------|-------|
| `clearskies-realtime` service: SSE bridge from weewx loop packets | ⬜ | `realtime-dev` + `test-author` | Minimal, focused, ~few hundred lines of Python |
| Wire dashboard to live SSE updates | ⬜ | `dashboard-dev` | Replace polling with `EventSource` |
| Spin up an ephemeral `weather-deploy-rehearsal` LXD container on Ratbert | ⬜ | lead (Opus) | Separate from the long-lived `weather-dev` container (stood up in Phase 1). Pristine container that gets the public docs treatment — mirrors what a new user would do, cross-machine, manual gates. Tear down + rebuild for each rehearsal pass. |
| Full end-to-end deploy rehearsal there using the public docs only | ⬜ | lead (Opus) + `auditor` | If the public docs don't work, the public docs are wrong — fix them |
| Polish, accessibility audit, perf audit | ⬜ | `dashboard-dev` + `auditor` | Lighthouse, axe |
| Documentation completeness review | ⬜ | `docs-author` + `auditor` | All `README`/`INSTALL`/`CONFIG` polished |

### Phase 5 — UAT & cutover

| Task | Status | Notes |
|------|--------|-------|
| Open `next.weather.shaneburkhardt.com` for UAT | ⬜ | Apache vhost on cloud + Let's Encrypt cert; basic-auth or API-key gated while beta |
| Verify against production-equivalent data for a full week | ⬜ | Catch periodic-job bugs (daily/weekly aggregations) |
| DNS/Apache cutover: `weather.shaneburkhardt.com` → new stack | ⬜ | Atomic-ish; old Belchertown setup archived but not deleted |
| Retire the `next.` subdomain | ⬜ | After cutover successful for a week |
| Public release: GitHub repos go public, weewx forum announcement | ⬜ | Coordinate with the documentation-completeness pass below |
| Documentation-completeness pass | ⬜ | Final polish before public eyes see it |

### Phase 6 — Iterate (optional/ongoing)

| Task | Status | Notes |
|------|--------|-------|
| Community feedback triage | ⬜ | Issue templates, discussion forum |
| Optional: extract `clearskies-design-tokens` to its own npm package | ⬜ | Only if anyone asks |
| Optional: PWA (installable, offline-capable) | ⬜ | Service worker, manifest |
| Optional: mobile companion app on the same API | ⬜ | React Native or Capacitor wrap of the SPA |
| Optional: HA custom integration repo | ⬜ | Only if YAML-via-docs proves insufficient |

---

## Open decisions

Authoritative status of every ADR (Pinned / Proposed / Accepted) lives in [docs/decisions/INDEX.md](../decisions/INDEX.md). This plan does not duplicate that list.

One non-ADR doc task: INSTALL docs must include a matrix of supported weewx environments (native Debian/Ubuntu, LXD container, Docker, Proxmox VM, Raspberry Pi) with the recommended install path for clearskies-api and clearskies-realtime in each.

### Parking-lot follow-ups (non-blocking)

- **Tighten the units lookup against canonical-data-model §2.1.** During 3a-1 round 4, three `_GROUP_MEMBERS` misclassifications (`snow` missing from group_rain, `snowRate` in group_rain not group_rainrate, `sunshineDur` missing from group_deltatime) were caught by test-author iterating the §2.1 table against the live constant. The fixes are mechanical, but the underlying drift risk recurs whenever §2.1 grows. Generate `_GROUP_MEMBERS` from a single source of truth (parsed §2.1 table at startup, OR a Python constant that mirrors §2.1 verbatim with a CI test asserting parity). Not 3a-1 scope; queue for a future cleanup round in Phase 2 or Phase 4.

---

## Risks & mitigations

| Risk | Mitigation |
|------|-----------|
| Scope creep — "Apple Weather aesthetic" is hard | Phase 1 design-direction document is the contract; Phase 3 reviews against it |
| Long build, no visible progress | Phase 3 uses mock data so the UI is browser-visible from Day 1; user can react early |
| Documentation rot | Documentation is a per-phase acceptance gate, not a backlog item |
| Breaking weewx compatibility | API targets the *stable* weewx schema; covered by tests against multiple weewx versions in CI |
| Letting GPL v3 deter contributions | Counter-balanced by ecosystem alignment with weewx; document the rationale in `LICENSE-RATIONALE.md` |
| Solo maintenance burden after launch | `weewx-stack` docs explicitly invite forks; no commercial dependence on growth |

---

## Decision log

- **2026-04-29** — predecessor `WEATHER-EVALUATION-PLAN.md` archived. Project pivoted from "evaluate alternative weewx skins" to "build new modern stack." Driver: every weewx-ecosystem skin reads as visually amateurish; lateral move would not solve the redesign goal. Rule captured at [rules/weather-skin.md](../../rules/weather-skin.md).
- **2026-04-29** — five-component breakdown adopted. EMQX retained for HA/MQTT power-user channel; `clearskies-realtime` introduced as separate browser-only SSE channel.
- **2026-04-29** — license set to GPL v3 to mirror weewx.
- **2026-04-29** — name "Clear Skies" verified clear in weewx ecosystem (no skin/repo conflicts).
- **2026-04-29** — `clearskies-design-tokens` deferred to Phase 6+; tokens exist internally from day 1 but aren't published as a standalone package until demand exists.
- **2026-04-29** — MQTT typo (`mgtt://` → `mqtt://`) confirmed already fixed on live server. Server-side data publication verified healthy. Browser-side live delivery verified by user. Original "regular users don't see live data" symptom fully resolved. [docs/archive/MQTT-TYPO-FIX-PLAN.md](../archive/MQTT-TYPO-FIX-PLAN.md) archived as superseded.
- **2026-05-01** — [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md) Accepted. One-door reverse-proxy architecture: all public traffic through a single web server; inner services bind to localhost by default; upstream provider calls originate server-side. Caddy bundled in the docker-compose distribution; user-supplied web server (Apache, nginx, Caddy) for native installs.
- **2026-05-01** — [ADR-008](../decisions/ADR-008-auth-model.md) Accepted. No end-user authentication built in (consistent with weewx skin ecosystem); users add proxy auth as a deployment choice. Optional shared-secret header for cross-host proxy↔inner-service deploys. Service warns but does not refuse to start when bound to non-loopback without a secret.
- **2026-05-01** — [ADR-027](../decisions/ADR-027-config-and-setup-wizard.md) Accepted. ConfigObj/INI `.conf` files at `/etc/weewx-clearskies/`; secrets in `secrets.env` (mode 0600); on-demand standalone web configuration UI in `weewx-clearskies-stack` (HTTPS by default with self-signed cert and printed fingerprint, admin user/password auth); no bind-range filter (operator's network, operator's call); IPv4/IPv6 dual-stack; disable-able for power users via `[ui] enabled = false`.
- **2026-05-01** — Project-wide IPv4/IPv6-agnostic coding rule landed in [rules/coding.md](../../rules/coding.md) Section 1: governs the configuration UI's listener, the api/realtime listeners, outbound provider calls, and any future networking code. Concrete dos/don'ts: dual-stack defaults, `getaddrinfo` instead of `gethostbyname`, `ipaddress` library instead of regex IP validation, brackets around IPv6 in URLs, `INET`/`VARBINARY(16)` for storage.
- **2026-05-01** — [ADR-010](../decisions/ADR-010-canonical-data-model.md) Accepted. 8 entity types (Observation, ArchiveRecord, HourlyForecastPoint, DailyForecastPoint, ForecastDiscussion, AlertRecord, AQIReading, StationMetadata) + 2 containers (ForecastBundle, AlertList); weewx-aligned camelCase JSON keys / snake_case Python field names via Pydantic alias_generator; canonical units = weewx's configured `target_unit` system with units metadata block in every response; UTC ISO-8601 with `Z` suffix; all-fields-optional with explicit nulls; single-station type model; custom weewx columns flow through `extras` slot; prose captured at three layers (weatherText per point, narrative per day, ForecastDiscussion per bundle). Full per-field catalog deferred to `docs/contracts/canonical-data-model.md` (Phase 1 deliverable alongside OpenAPI spec).
- **2026-05-01** — [ADR-011](../decisions/ADR-011-multi-station-scope.md) Accepted. v0.1 ships single-station only; multi-station explicitly out-of-scope ("not planned, revisit if concrete demand surfaces"). One API ↔ one weewx archive DB ↔ one StationMetadata ↔ one realtime stream ↔ one dashboard. Forward-compat path is non-breaking per ADR-010: optional `stationId` per record, optional `?station=<id>` query param, list `StationMetadata`. No tenant model in auth ([ADR-008](../decisions/ADR-008-auth-model.md)) or configuration UI ([ADR-027](../decisions/ADR-027-config-and-setup-wizard.md)).
- **2026-05-02** — 12-category content walk complete. Output: [docs/reference/CLEAR-SKIES-CONTENT-DECISIONS.md](../reference/CLEAR-SKIES-CONTENT-DECISIONS.md). New cross-cutting threads locked: PWS-contributor track as default lens for "free" providers, internal plug-in-style provider modules ([ADR-038](../decisions/INDEX.md) Pinned), distribution mechanism open thread ([ADR-039](../decisions/INDEX.md) Pinned), render-time sensor-availability detection (per-station-adaptive rendering), user-defined records distinct from custom charts, i18n first-class for v0.1 (13 locales from numisync.com), accessibility WCAG 2.1 AA load-bearing project-wide (rules at [rules/coding.md](../../rules/coding.md) §5). Two new artifacts: [NOAA-COOP-CWOP-REPORTING-RESEARCH.md](../reference/NOAA-COOP-CWOP-REPORTING-RESEARCH.md), [CLEAR-SKIES-CONTENT-DECISIONS.md](../reference/CLEAR-SKIES-CONTENT-DECISIONS.md).
- **2026-05-02** — [ADR-009](../decisions/ADR-009-design-direction.md) Accepted. Multi-page card-based dashboard with icon-rail nav; three-tier information hierarchy (always-visible / tile-body / click-to-expand); operator-uploadable hero images with event-trigger system (default / active alert / weather condition / date range / season / time-of-day) + shipped generic in-house-authored graphic default (NOT photography, license-clean); Inter font with tabular figures for live data; neutral-foundation palette + one operator-picked accent + EPA AQI scale + semantic alert/warning/success/info colors; all three theme modes (light / dark / auto-by-sunrise-sunset / auto-by-OS); restrained motion respecting `prefers-reduced-motion`; mobile-first non-negotiable; WCAG 2.1 AA throughout. Operator licensing acknowledgment required at every image upload.
- **2026-05-02** — [ADR-024](../decisions/ADR-024-page-taxonomy.md) Accepted. Nine built-in pages — Now (`house`), Forecast (`cloud-sun-rain`), Charts (`chart-line`), Almanac (`moon`), Earthquakes (`activity`), Records (`trophy`), Reports (`file-text`), About (`info`), Legal (`scale`) — each individually hide-able by operator. Custom-page mechanism: operator picks slug + name + Lucide icon (from a curated subset, Phase 3) + nav position + content blocks (canonical cards + markdown narrative + custom charts/records/media). Marine deferred per cat 7. Configuration UI is NOT in the public dashboard nav (separate per ADR-027). Default per-page card composition synthesized from the 12-category content walk. Reports page setup-time precondition: dashboard checks for `/NOAA/*.txt` files at startup; absent → page hidden + operator prompted to enable weewx NOAA generator.
- **2026-05-02** — [ADR-038](../decisions/ADR-038-data-provider-module-organization.md) Accepted. Project-wide pattern: every external data source (forecast / AQI / alerts / earthquakes / radar) is a plugin module in clearskies-api at `weewx_clearskies_api/providers/{domain}/{provider}.py`. Five module responsibilities, capability registry, error taxonomy. **Clear Skies ships ZERO weewx extensions.**
- **2026-05-02** — [ADR-013](../decisions/ADR-013-aqi-handling.md) Accepted. AQI providers as clearskies-api plugin modules per ADR-038. Day-1 set: Aeris / OpenMeteo / OpenWeatherMap / IQAir. Two operator paths: (A) own weewx extension writing custom columns mapped via [ADR-035](../decisions/ADR-035-user-driven-column-mapping.md), (B) clearskies-api AQI plugin. Historical AQI persisted by clearskies-api (mechanism = Phase 2). AQI alerts via NWS pipeline (AQA / AS_Y).
- **2026-05-02** — [ADR-035](../decisions/ADR-035-user-driven-column-mapping.md) Accepted. Schema introspection + auto-map stock weewx columns + operator maps non-stock at setup. AQI is the worked example. Persists in operator config; re-mappable via configuration UI.
- **2026-05-02** — [ADR-014](../decisions/ADR-014-almanac-data-source.md) Accepted. Skyfield (not pyephem) for all almanac calculations; server-side; NASA JPL DE421 ephemerides.
- **2026-05-02** — [ADR-026](../decisions/ADR-026-accessibility-commitments.md) Accepted. WCAG 2.1 Level AA project-wide; per-change audit + pre-ship audit; release-blocking. Implementation rules in [rules/coding.md](../../rules/coding.md) §5.
- **2026-05-02** — [ADR-021](../decisions/ADR-021-i18n-strategy.md) Accepted. 13 locales for v0.1 (en/de/es/fil/fr/it/ja/nl/pt-PT/pt-BR/ru/zh-CN/zh-TW); no RTL. Framework / extraction tooling deferred to Phase 2–3.
- **2026-05-02** — [ADR-023](../decisions/ADR-023-light-dark-mode-mechanism.md) Accepted. `data-theme` attribute on `<html>`; React theme provider resolves operator default + user override; auto-sunrise-sunset uses api-supplied sunrise/sunset; auto-OS uses `prefers-color-scheme`.
- **2026-05-02** — [ADR-022](../decisions/ADR-022-theming-branding-mechanism.md) Accepted. CSS variables on `:root` / `[data-theme="dark"]`; curated 6-entry accent palette (no free-form picker — protects WCAG AA); operator logo upload (light + optional dark, auto-invert with warning); `custom.css` escape hatch documented as best-effort. No Cheetah hooks.
- **2026-05-02** — [ADR-015](../decisions/ADR-015-radar-map-tiles-strategy.md) Accepted. Leaflet + OSM base. 8 day-1 radar provider modules in `weewx_clearskies_api/providers/radar/` (rainviewer/openweathermap/aeris/iem_nexrad/noaa_mrms/msc_geomet/dwd_radolan/mapbox_jma) + `iframe` config slot. Setup wizard suggests by lat/lon. clearskies-api proxies keyed providers server-side per ADR-037. OWM labeled "Model precipitation," not real radar.
- **2026-05-02** — [ADR-018](../decisions/ADR-018-api-versioning-policy.md) Accepted. URL-path versioning (`/api/v1/...`); major bump only on breaking changes; **no support-window promise** — software is AS-IS under GPL v3 ([ADR-003](../decisions/ADR-003-license.md)); `Deprecation`/`Sunset` headers (RFC 8594) are advisory technical signals, not commitments. All errors use RFC 9457 `application/problem+json`.
- **2026-05-02** — [ADR-019](../decisions/ADR-019-units-handling.md) Accepted. Server passes weewx `target_unit` through with units metadata block per ADR-010; no server-side conversion; no per-user override at v0.1 (Phase 6+ enhancement via localStorage + metadata block). Operator picks units in `weewx.conf` before installing.
- **2026-05-02** — [ADR-020](../decisions/ADR-020-time-zone-handling.md) Accepted. UTC on wire; station-local TZ for display (visitors see the station's clock, not their browser's); IANA TZ identifier in StationMetadata; source priority = operator config → weewx config → derived from lat/lon. Browser uses `Intl.DateTimeFormat`. No per-user override at v0.1.
- **2026-05-02** — [ADR-029](../decisions/ADR-029-logging-format-destinations.md) Accepted. Structured JSON one-line-per-record to stdout (12-factor); required fields `timestamp`/`level`/`logger`/`message`/`request_id`; stdlib `logging` + small JSON formatter (no `structlog` dep); `logging.Filter` strips auth headers, API keys, SQL parameter values; capture via `journalctl` or `docker logs`; log shipping is operator deploy concern.
- **2026-05-02** — [ADR-030](../decisions/ADR-030-health-check-readiness-probes.md) Accepted. `/health/live` + `/health/ready` on a separate loopback port (default 8081 for api); degraded → 200 (no orchestrator restart loops on transient provider failures); 503 only when service genuinely cannot serve; unauthenticated by virtue of loopback default. Provider readiness signals route through ADR-038 capability registry.
- **2026-05-02** — [ADR-012](../decisions/ADR-012-database-access-pattern.md) Accepted. SQLAlchemy 2.x; read-only DB user enforced both at the database (`SELECT`-only grant) AND a startup write-probe that refuses to start the service if write permissions exist; SQLite uses `?mode=ro` URI; runtime schema reflection populates the column registry that ADR-035 mapping consumes; per-request session via FastAPI DI.
- **2026-05-02** — [ADR-017](../decisions/ADR-017-provider-response-caching.md) Accepted. Pluggable cache backend: `memory` default (zero deps; LRU+TTL via `cachetools`), `redis` optional (configurable via `CLEARSKIES_CACHE_URL`). uvicorn ships single-worker by default; multi-worker deploys MUST use Redis or burn API quotas N×. Per-provider TTL declared in module capability per ADR-038.
- **2026-05-02** — [ADR-016](../decisions/ADR-016-severe-weather-alerts.md) Accepted. Alerts as their own clearskies-api domain (`weewx_clearskies_api/providers/alerts/`). Day-1 set: `nws` (US, free), `aeris` (US/CA/EU, PWS-contributor track), `openweathermap` (global, paid One Call 3.0). Single source per deploy; setup wizard suggests by lat/lon. Operators in uncovered regions see no banner (cat 10 sensor-availability). NWS AQA/AS_Y AQI alerts ride this pipeline.
- **2026-05-02** — Phase 2 ADR backlog closed. All Phase 2 blocking ADRs (007, 010, 011, 012, 013, 016, 017, 018, 019, 020, 027, 029, 030, 035, 037, 038) are Accepted.
- **2026-05-02** — [ADR-025](../decisions/ADR-025-browser-support-matrix.md) Accepted. Modern evergreen browsers, last 2 years (~Chrome 110+, Firefox 110+, Safari 16.4+, iOS Safari 16.4+, Android Chrome). Browserslist `>0.5%, last 2 years, not dead, not op_mini all`. No IE, no pre-ES2022 baseline.
- **2026-05-02** — [ADR-033](../decisions/ADR-033-performance-budget.md) Accepted. Targets (NOT release gates): Lighthouse Performance ≥ 90 on primary pages; CWV "Good" thresholds (LCP ≤ 2.5s / INP ≤ 200ms / CLS ≤ 0.1); Now-page initial JS bundle ≤ 200 KB gzipped; p95 API latency per endpoint class. Misses are documented in `docs/audits/<release>.md` and the release ships anyway. Contrast with ADR-026 a11y, which IS release-blocking.
- **2026-05-02** — [ADR-031](../decisions/ADR-031-observability-metrics.md) Accepted. Logs ([ADR-029](../decisions/ADR-029-logging-format-destinations.md)) are the default observability surface; Prometheus `/metrics` is opt-in via `CLEARSKIES_METRICS_ENABLED=true`, served on the health port (loopback default per ADR-030). HTTP/provider/cache/DB metrics with bounded cardinality. OpenTelemetry deferred to Phase 6+.
- **2026-05-02** — [ADR-032](../decisions/ADR-032-versioning-across-repos.md) Accepted. Independent SemVer per repo (no lockstep); pre-1.0 minor bumps may ship breaking changes; major-bump triggers named per repo (api breaking change → api major; SSE event format change → realtime major; config schema break → dashboard major; matrix bump → stack major). Cross-repo compatibility matrix in `clearskies-stack` README. ADR-018 (api wire contract) and ADR-032 (repo release lifecycle) deliberately separate concerns.
- **2026-05-02** — [ADR-034](../decisions/ADR-034-deployment-topology-default.md) Accepted. Default topology: single-host, co-located with weewx. Two install paths both single-host — native (`pip install` + systemd, operator's existing web server) for existing weewx operators, or `clearskies-stack` docker-compose (Caddy-bundled, auto-LE) for new operators. weewx itself NOT bundled in compose. Multi-host supported but reference-only.
- **2026-05-02** — Process rule updates ([rules/clearskies-process.md](../../rules/clearskies-process.md)): item 7 — corrections to Accepted ADRs edit in place (no supersession clutter). Conciseness rule added — standard ADRs ~80 lines, parent-pattern ADRs ~150 lines. Cleanup pass on bloated existing ADRs (ADR-009, ADR-038) added to Phase 1 task table.
- **2026-05-04** — [ADR-039](../decisions/ADR-039-distribution-installation-mechanism.md) Accepted. Three distribution channels: PyPI (`pip install weewx-clearskies-api`/`-realtime`), GHCR/Docker Hub container images (api/realtime/dashboard, used by `clearskies-stack` docker-compose), and GitHub Releases source tarballs. Per-OS: Linux native or Docker; macOS native or Docker; Windows = Docker Desktop, full stop. No MSI / .pkg / AppImage / Snap / Flatpak at v0.1 — bespoke OS installers are a maintenance black hole for a single-maintainer GPL project. Sibling [ADR-028](../decisions/ADR-028-update-mechanism.md) (update mechanism) still Pinned at this point.
- **2026-05-04** — [ADR-028](../decisions/ADR-028-update-mechanism.md) Accepted. Updates use the same channel as install: `pip install -U weewx-clearskies-api`/`-realtime` for native (restart unit), `docker compose pull && docker compose up -d` for Docker, source-tarball reinstall for source builds. No in-app self-update, no auto-update daemon (Watchtower etc.) at v0.1. CHANGELOG.md per repo is the single source of upgrade-relevant info; cross-repo compatibility matrix lives in `clearskies-stack/README.md` per ADR-032. AS-IS posture inherited from ADR-018: no LTS, no security backports, no EOL schedule. Configuration preservation locked in: native pip doesn't touch `/etc/weewx-clearskies/`; Docker compose bind-mounts the config dir; config-file schema changes are always CHANGELOG-flagged, never silent. **Phase 1 ADR backlog closed** — every Pinned slot in INDEX.md is now Accepted. Next Phase 1 task: cleanup pass on bloated ADRs per the conciseness rule.
- **2026-05-04** — Multi-agent setup landed for Phase 2+ coding work. Pattern: Claude Code Agent Teams (experimental, gated by `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`). Lead = Opus session. Roster of 6 custom agents in `.claude/agents/`: `clearskies-api-dev` (Sonnet, FastAPI/SQLAlchemy backend), `clearskies-dashboard-dev` (Sonnet, React/Tailwind/shadcn/Tremor SPA), `clearskies-realtime-dev` (Sonnet, SSE bridge), `clearskies-test-author` (Sonnet, pytest/Playwright/axe-core; integration tests run against the docker-compose dev/test stack with both runtime backends per [ADR-012](../decisions/ADR-012-database-access-pattern.md)), `clearskies-docs-author` (Sonnet, README/INSTALL/CONFIG/SECURITY/DEVELOPMENT/CHANGELOG), and `clearskies-auditor` (Opus — dedicated review-only role; cites ADR-NNN per finding; never implements). Env var enabled in `~/.claude/settings.json`. Phase 2/3/4 task tables annotated with teammate assignments. Known limitations accepted: no `/resume` with active in-process teammates, one team per session, no nested teams, Windows = in-process mode only (split-pane needs tmux/iTerm2). Fallback if pattern doesn't work in practice: revert to subagent orchestration (Pattern A). Auditor role added at user request to cover the case where peer-to-peer teammate coordination doesn't always route through the lead — the auditor is a "check the work" guarantor regardless of how teammates communicate among themselves.
- **2026-05-04** — Phase 1 task added: docker-compose dev/test stack with MariaDB + backend-agnostic Python seed loader, sourced from a 30–60 day production weewx archive snapshot. Same dataset loads into both runtime backends per [ADR-012](../decisions/ADR-012-database-access-pattern.md); CI matrix runs every integration test against both. Output lands in `clearskies-stack/dev/`. Driver: testing against a different backend than we deploy on is testing-the-wrong-thing; keeping SQLite + MariaDB at runtime means we have to actually validate both. Weather observation data is public-by-design (temp/humidity/wind/rain on weather.shaneburkhardt.com, lat/lon broadcast to CWOP) so no sanitization required. Sequencing: parallel with the spike, both before the OpenAPI contract task.
- **2026-05-04** — Phase 1 task 1 (tech-stack spike) **complete**. ADR-002 stack validated end-to-end inside `weather-dev`: Vite 8 + React 19 + TS 6 + Tailwind v4 + shadcn v4 + Recharts 3.8 + Lucide. Production bundle 164.52 KB gzipped — fits [ADR-033](../decisions/ADR-033-performance-budget.md)'s 200 KB budget with ~35 KB headroom on a Now-page mock with one chart on screen. Two scaffold-time footguns confirmed and documented for the eventual dashboard repo: (1) `"overrides": { "react-is": "^19.2.0" }` in `package.json` before installing Recharts (without it, install fails with ERESOLVE on React 19); (2) `"ignoreDeprecations": "6.0"` in `tsconfig.app.json` to silence the TS6 `baseUrl` deprecation warning that shadcn's CLI scaffold emits. Recharts wrapper components must include a screen-reader-only `<table>` fallback to satisfy [ADR-026](../decisions/ADR-026-accessibility-commitments.md) / `rules/coding.md` §5.5. No ADR amendments needed — every ADR-002 choice was confirmed correct. Findings: [docs/reference/SPIKE-FINDINGS.md](../reference/SPIKE-FINDINGS.md).
- **2026-05-04** — Plan-vs-ADR audit completed. Drift fixed in plan body: architecture diagram, components table, tech stack table, security baseline section, versioning section, coexistence section, and Phase 1 task 1 description all updated to match the 39 Accepted ADRs verbatim or to defer to them as authoritative pointers. Trigger: Phase 1 spike (task 1) was built against the plan body's stale tech-stack table (Tremor + ECharts) when ADR-002 had already locked shadcn + Recharts. Audit findings + every drift fixed: [docs/reference/PLAN-VS-ADR-AUDIT-2026-05-04.md](../reference/PLAN-VS-ADR-AUDIT-2026-05-04.md). New process sub-rule landed in [rules/clearskies-process.md](../../rules/clearskies-process.md) "Read the ADR before the plan." Spike artifact + first-pass findings preserved in [docs/reference/SPIKE-FINDINGS.md](../reference/SPIKE-FINDINGS.md); second pass against the ADR-002-correct stack (Recharts) is the next Phase 1 task.
- **2026-05-04** — `weather-dev` LXD container stood up on ratbert at `192.168.2.113` (DHCP/SLAAC on `br-vlan2`). Ubuntu 24.04, `security.nesting=true` (Docker-in-LXC), 6 GB memory cap. Provisioned with Docker Engine + Compose, Node 22 LTS, Python 3.12, uv, git, build-essential. **Brought forward from Phase 4 to Phase 1** because the Windows workstation (DILBERT) is a misfit for the project's runtime stack — Docker Desktop heavyweight, no native Linux containers, Playwright/axe-core Linux-first, integration tests want a real systemd-style environment. Rule landed in [rules/clearskies-process.md](../../rules/clearskies-process.md) "Dev/test runs in `weather-dev`": DILBERT is for editing/git/orchestration only; all `docker compose`, `pytest`, `npm`, `vite`, etc. runs inside the container. Phase 4 entry refactored — that container is a separate ephemeral `weather-deploy-rehearsal` instance for the deploy-against-public-docs pass.
- **2026-05-04** — Phase 1 task complete: docker-compose dev/test stack scaffolded at [`repos/weewx-clearskies-stack/dev/`](../../repos/weewx-clearskies-stack/dev/). MariaDB 10.11 service + reflection-based snapshot capture (`snapshot/capture.py`, host-side) + backend-agnostic Python seed loader (`seed/seed_loader.py`, containerized). Same captured dataset loads into MariaDB or SQLite per ADR-012 via compose profiles. Smoke-tested loader against SQLite with synthetic fixture (3-row archive, null-coercion + short-row trailing-empty handling); MariaDB path will be exercised end-to-end first CI run. Drove a real defect found during smoke test: `_coerce_row` was silently truncating short rows via `zip` — fixed in place with defensive padding. Repo not yet `git init`'d (Phase 1 task 5 covers that). Layout matches [ADR-036](../decisions/ADR-036-workspace-layout.md) — files sit ready under `repos/` for the eventual repo-stand-up. **Follow-up validation pass inside `weather-dev`** drove two further defects fixed in place: invalid `pip install --require-hashes=false` Dockerfile syntax (`--require-hashes` is a flag, not value-taking), and SQLite-volume permission collision with the non-root `USER seed` directive (the seeder is transient dev/test data-loading infra, so root-in-container is appropriate; the non-root pattern stays mandatory for the runtime API/realtime services). Both backend profiles now load + verify against synthetic fixture end-to-end.
- **2026-05-05** — Phase 1 task: **API contract committed.** [`docs/contracts/openapi-v1.yaml`](../contracts/openapi-v1.yaml). OpenAPI 3.1, 23 paths, 53 schemas; validates clean. Drove two decision artifacts: (a) [ADR-040](../decisions/ADR-040-earthquake-providers.md) Accepted — earthquake providers as plugin modules per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md); day-1 set usgs/geonet/emsc/renass; single source per deploy; setup wizard suggests by region; USGS = global fallback. Mirrors [ADR-016](../decisions/ADR-016-severe-weather-alerts.md). Research: [EARTHQUAKE-PROVIDER-RESEARCH.md](../reference/EARTHQUAKE-PROVIDER-RESEARCH.md). (b) [ADR-010](../decisions/ADR-010-canonical-data-model.md) re-Accepted with `EarthquakeRecord` added (per item-7 in-place correction). Required: `id`, `time`, `latitude`, `longitude`, `magnitude`, `source`. Optional: `depth`, `magnitudeType`, `place`, `url`, `tsunami`, `felt`, `mmi`, `alert` (USGS PAGER), `status`, `extras`. Entity count now 9 cores + 2 containers. Process notes: realtime SSE deliberately not in this OpenAPI — separate `weewx-clearskies-realtime` contract; `/reports/{year}/{month}` returns raw weewx text (dashboard parses client-side); pagination supports both cursor and page-number forms; standalone `/units` endpoint dropped (units block embedded per response).
- **2026-05-05** — Phase 1 task: **CI scaffolding landed across all five repos.** Phase 1 minimal set per agreed scope: DCO sign-off check + gitleaks secret scan + dependency audit, three workflows per repo. Third-party actions SHA-pinned per [`coding.md`](../../rules/coding.md) §1; gitleaks installed via tarball download to avoid the gitleaks-action org-license requirement. Dependency-audit workflows are placeholder-tolerant (skip quietly when no manifest exists yet) so they're useful from day one without rotting. Lint, test, release-on-tag, and the [ADR-012](../decisions/ADR-012-database-access-pattern.md) DB-backend matrix for api are explicitly deferred to Phase 2/3/4 — there's no code yet to exercise them, and writing rotting placeholder steps that "echo no tests yet" creates noise without value. **Sync-script reshape:** `scripts/sync-to-weather-dev.ps1` rewritten as `scripts/sync-to-weather-dev.sh`. PowerShell-native ssh.exe was hitting a stale IdentityFile pointer in the user's Windows OpenSSH config (left over from another project); bash-side ssh works fine and is what the Bash tool / git-bash use. Same pass added `sudo -u ubuntu` to the in-container `git pull` to avoid git's "dubious ownership" check (repos are ubuntu-owned, but `lxc exec` defaults to root). Verified end-to-end against all five working trees in weather-dev.
- **2026-05-05** — Phase 1 task: **5 GitHub repos stood up.** All five Clear Skies repos created public under `github.com/inguy24/` per [ADR-004](../decisions/ADR-004-repo-naming.md) and [ADR-036](../decisions/ADR-036-workspace-layout.md): `weewx-clearskies-api`, `weewx-clearskies-realtime`, `weewx-clearskies-dashboard`, `weewx-clearskies-stack`, `weewx-clearskies-design-tokens`. Each repo: `README.md` placeholder (real content lands at the relevant Phase per [ADR-001](../decisions/ADR-001-component-breakdown.md)), `LICENSE` (GPL v3), `SECURITY.md` placeholder pointing back to the meta-repo security baseline, language-appropriate `.gitignore`. Stack repo carries the existing `dev/` docker-compose content from the prior Phase 1 task. Design-tokens is a name-reservation placeholder (does not violate ADR-001's Phase 6+ deferral — placeholder ≠ implementation work). Meta-repo `.gitignore` updated to exclude `repos/*` (with `!repos/.gitkeep` exception) to avoid double-tracking nested git repos; `repos/weewx-clearskies-stack/` content untracked from meta via `git rm --cached -r`. [`weather-clearskies.code-workspace`](../../weather-clearskies.code-workspace) created at meta root with all six folders (meta + 5 children). DILBERT→weather-dev sync mechanism: manual fire of [`scripts/sync-to-weather-dev.sh`](../../scripts/sync-to-weather-dev.sh) over `ssh ratbert "lxc exec weather-dev ..."`. Stale "pick git-clone or bind-mount" language in [rules/clearskies-process.md](../../rules/clearskies-process.md) — never discussed with user — corrected to record the actual mechanism. Webhook auto-sync ruled out (no public-facing inbound on the LXD host). One-time follow-up: clone the five repos into `weather-dev:/home/ubuntu/repos/`.
- **2026-05-05** — Phase 1 task: **Security baseline document committed.** [`docs/contracts/security-baseline.md`](../contracts/security-baseline.md). Per-component checklist (api / realtime / dashboard / stack) consolidating 7 source ADRs + `coding.md` §1 + cross-cutting controls not pinned to any single ADR. CI gating matrix per repo. §8 surfaces six known gaps; three with real teeth (multi-worker rate-limit storage requirement, request-body limit default, markdown-sanitization regression risk via `react/no-danger` allowlist). User pressure-tested [ADR-030](../decisions/ADR-030-health-check-readiness-probes.md) split-port architecture during review — confirmed keep-as-is; the loopback-by-default of [ADR-037](../decisions/ADR-037-inbound-traffic-architecture.md) makes the security argument equal either way, and the operational friction is small enough to revisit during Phase 2 if real implementation makes it visible. No ADR cascade triggered.
- **2026-05-05** — [ADR-010](../decisions/ADR-010-canonical-data-model.md) Naming sub-decision corrected in place per item-7 lifecycle. Switched from snake_case Python + camelCase JSON (with Pydantic `alias_generator` bridge) to **camelCase everywhere — single name per field, identical in Python attribute and JSON key.** Reason: two-name overhead caused real friction during user review with no consumer benefiting from the snake_case form; weewx itself uses camelCase, so this is more weewx-aligned, not less. Pydantic config simplified (drops `alias_generator` / `populate_by_name` / `by_alias`). Per-file ruff `N815` suppression for `weewx_clearskies_api/models/canonical.py`. Spec [`docs/contracts/canonical-data-model.md`](../contracts/canonical-data-model.md) §1 and §5 updated to match. Same pass added internal-only header callouts to §2 (weewx unit-system reference) and §4 (provider→canonical mapping tables) — both are normalizer-internal lookups, never exposed on the wire — to reduce reader confusion between wire-format concepts and implementation internals. Wire format itself unchanged.
- **2026-05-05** — Phase 1 task: **Canonical-data-model spec committed.** [`docs/contracts/canonical-data-model.md`](../contracts/canonical-data-model.md). Companion to [ADR-010](../decisions/ADR-010-canonical-data-model.md) and the OpenAPI contract — fills the per-field unit table OpenAPI deliberately defers. Three load-bearing parts: (1) full per-field enumeration for every canonical entity (with weewx-source columns and provider-source fields tagged); (2) per-field unit mapping for each weewx `target_unit` system (US / METRIC / METRICWX), driven by [`docs/reference/weewx-5.3/reference/units.md`](../reference/weewx-5.3/reference/units.md); (3) provider→canonical mapping tables for the day-1 forecast (aeris/nws/openmeteo/openweathermap/wunderground per [ADR-007](../decisions/ADR-007-forecast-providers.md)), AQI (aeris/openmeteo/openweathermap/iqair per [ADR-013](../decisions/ADR-013-aqi-handling.md)), alerts (nws/aeris/openweathermap per [ADR-016](../decisions/ADR-016-severe-weather-alerts.md)), and earthquake (usgs/geonet/emsc/renass per [ADR-040](../decisions/ADR-040-earthquake-providers.md)) providers. Radar [ADR-015](../decisions/ADR-015-radar-map-tiles-strategy.md) coverage notes that tile data has no canonical-field mapping — capability declarations carry the per-provider URL templates. Self-audit findings surfaced in-reply, three fixed in place: `appTemp` removed from first-class observation table (kept as promotion candidate, currently in `extras` until OpenAPI v1.x bump); µg/m³ → ppm conversion formula corrected (`ppm = µg/m³ × 24.45 / molecular_weight`); §2.2 added flagging that operator overrides via `[StdConvert]` change the per-field unit and the api startup must read the actual configured unit per group, not just the system label.
- **2026-05-04** — Cleanup pass on bloated ADRs **complete**. Conciseness rule clarified in [rules/clearskies-process.md](../../rules/clearskies-process.md): bloat is measured by content density, not line count. Orphan `ADR-035-observation-entity-model.md` deleted (was a Pinned placeholder superseded by `user-driven-column-mapping.md` but never cleaned up). Nine ADRs trimmed in place per ADR-process item 7 (status flipped Proposed → re-approved → Accepted with date 2026-05-04): ADR-008 (auth-model), ADR-009 (design-direction), ADR-010 (canonical-data-model), ADR-011 (multi-station-scope), ADR-024 (page-taxonomy), ADR-027 (config-and-setup-wizard), ADR-036 (workspace-layout), ADR-037 (inbound-traffic-architecture), ADR-038 (data-provider-module-organization). Cuts removed: redundant prior-ADR recap sections, multi-paragraph option-defense prose, "what this ADR is NOT" enumerations, audit findings that restated obvious tradeoffs, Phase 2/3 implementation code blocks. Decisions, options preserved with one-line verdicts, load-bearing implementation facts, and out-of-scope guards retained verbatim. Remaining 28 ADRs audited paragraph-by-paragraph for content density and confirmed tight — no filler, every paragraph load-bearing. All 39 ADRs in INDEX.md now meet the conciseness rule.
- **2026-05-06** — **Phase 2 task 1 (FastAPI scaffold) complete.** Eight commits on `main` at github.com/inguy24/weewx-clearskies-api: initial scaffold (39 files, 3095 insertions), three pytest-surfaced fix commits (regex group reference bug — SQL pattern lacked group 1 but the redaction loop hardcoded `\g<1>` template; missing path-existence check in `load_settings` when explicit `config_path` is passed; Authorization regex stopping at first whitespace so two-word "Bearer xxx" only redacted "Bearer"), `uv.lock` follow-up, two dep-audit workflow fixes (Phase 1 `pip-audit --strict` audited the CI runner environment not project deps; fixed via `uv export --format requirements-txt --no-emit-project`), and a fastapi 0.115.12→0.136.1 / starlette 0.46.2→1.0.0 bump clearing two real CVEs (CVE-2025-54121, CVE-2025-62727). 73/73 pytest pass on `weather-dev`; both CI workflows (`gitleaks`, `dep-audit`) green. Multi-agent execution: api-dev (Sonnet) implementation, auditor (Opus) reviewed across 9 findings + a diff-only re-audit, lead (Opus) synthesis. Two process improvements landed in the rules: (1) [`rules/clearskies-process.md`](../../rules/clearskies-process.md) "Plain English when explaining decisions to the user" strengthened with a once-per-conversation definition rule and a 2026-05-06 incident `Why` block — user verbatim: *"you have been bombarding me with so much jargon, I cannot see straight"*; (2) [`CLAUDE.md`](../../CLAUDE.md) "Collaboration style" gained a corresponding cross-cutting bullet pointing at the detailed rule. Process feedback for future task rounds (carry into briefs): when an ADR conflicts with the task brief, STOP at the first conflict and message the lead — don't proceed-and-flag in the closeout. Task 1 cost a 16-site rename round because the dev surfaced two name conflicts in their closeout instead of stopping at the first one. The Phase 1 dep-audit workflow fix landed only in the api repo; the other four repos (realtime, dashboard, stack, design-tokens) still have the unfixed workflow but it skips cleanly while their content is placeholder, will need the same one-line fix when each lands its first real code commit. Branching policy decided in-session: no feature branches pre-1.0; commit straight to `main`/`master`; revisit when v0.1 ships.
- **2026-05-06** — **Phase 2 task 2 (read-only DB user enforcement) complete.** Implements [ADR-012](../decisions/ADR-012-database-access-pattern.md) end-to-end across two repos: `weewx-clearskies-api` (DB engine, write-probe, schema reflection, per-request session, DB readiness probe) and `weewx-clearskies-stack` (`dev/mariadb-init/01-clearskies-ro.sql` provisioning the SELECT-only `clearskies_ro` user idempotently; production-schema `tables.json` + 5-row `archive.csv` snapshot in `dev/snapshot/data/`). Multi-agent execution across three rounds: round 1 = parallel api-dev (Sonnet) + test-author (Sonnet); round 2 = api-dev fixes 2 real defects test-author's integration tests caught (writable-user-passes-probe via `IntegrityError` because the broad `DatabaseError` catch swallowed the NOT-NULL-constraint signal — security-critical; SQLite URL form `sqlite:////path?mode=ro&uri=true` worked for `engine.connect()` but failed for `MetaData.reflect()`, fixed to `sqlite+pysqlite:///file:///path?mode=ro&uri=true`); round 3 = api-dev remediates 5 auditor (Opus) findings (`pytest -m integration` selected zero tests because the marker was registered but no test was decorated with it; probe and engine messages pointed at `INSTALL.md` which does not exist — fixed by inlining the SQL grant recipe and pointing at `etc/api.conf.example`; `pool_size`/`max_overflow` knobs missing from `api.conf.example` despite ADR-012 naming them as configurable; reflection-non-fatal-startup paired with `/health/ready=200` would let an orchestrator route traffic into a service with an empty registry — flipped to fatal-exit matching the write-probe pattern; bare-IP heuristic in `_validate_db_host` misclassified `host:port` typos as malformed IPv6 — tightened with explicit typo guard plus stricter IPv4/IPv6 detection). Final pytest on `weather-dev`: 151 / 143 / 136 pass on MariaDB / SQLite / unit-only with zero failures and zero xfails; `pytest -m integration` selects 27. Process wins this task: (a) integration tests against the real production schema caught the writable-user defect that the unit-test suite (which used a synthetic 1-column `archive` table) missed — the dev's iteration past the lead's initial `IntegrityError`-only fix to also cover MariaDB error 1364 ("no default value") shows the value of testing against the real schema with multi-column NOT-NULL constraints; (b) the auditor's 5 findings were entirely source-side, distinct from the 2 runtime defects integration tests caught — confirming both audit modes are load-bearing, not redundant; (c) STOP-at-first-conflict discipline held — zero ADR conflicts surfaced across all three rounds; (d) lead-call discipline on F4 (fatal-exit vs extending the readiness probe with retry machinery) avoided adding deferred-rework risk and matched ADR-012's "refuse to start in known-bad states" spirit. Carry-forward for task 3: `tests/test_db_integration.py` lives at module-level `pytestmark = pytest.mark.integration`; CI matrix workflow (deferred from Phase 1) should gate on `pytest -m integration` to exercise both backends; the `clearskies_ro` user provisioning pattern in `dev/mariadb-init/` is the template for any future SQL-init dev-stack additions; `INSTALL.md` is still a docs-author task-8 deliverable — when it lands, the inline-SQL-recipe messages in `db/probe.py` and `db/engine.py` get updated to point at it.
- **2026-05-07** — **Phase 2 task 3a-2 (meta + static + pure-compute endpoints) complete.** 8 paths covering Almanac (3 paths, skyfield + DE421 ephemeris per [ADR-014](../decisions/ADR-014-almanac-data-source.md)) + Station (1 path, weewx.conf-backed metadata + first/last archive timestamps replacing task-1's hardcoded placeholder) + Capabilities (1 path, ColumnRegistry data + empty providers list until 3b populates per [ADR-038](../decisions/ADR-038-data-provider-module-organization.md)) + Pages (1 path, 9 built-ins from [ADR-024](../decisions/ADR-024-page-taxonomy.md) + operator-hideable via `[pages] hidden`) + Charts (1 path, 4 built-in groups with lead+user-confirmed default member sets, registry-pruned at request time) + Content (2 paths, markdown file passthrough; sanitization stays dashboard-side per security-baseline §5). Two rounds: round 1 = initial impl (~2450 LOC across 13 new files + 8 modified) and parallel test-author tests (250 test executions, 0 failures); round 2 = 4-finding remediation (F1 configobj-comma list-parse helper extracted from altitude-only fix and applied to location + station_type + timezone defensively; F2 day-window switched from UTC midnight to station-local midnight in `compute_almanac` + `daylightDeltaVsYesterdayMinutes` + sun-times year loop + moon-phases month/year loops to fix `daylightMinutes=0` at summer solstice for western-hemisphere stations; F3 nine bare `except Exception:` blocks in `services/almanac.py` removed — Loader paths narrowed to `(OSError, IOError, urllib.error.URLError, ValueError)`, per-day compute paths drop the catch entirely so polar-edge handling is `if events.size == 0:` not exception-driven and real bugs propagate to FastAPI's RFC 9457 handler; F4 `/station` "no such table" speculative branch dropped — production unreachable per [ADR-012](../decisions/ADR-012-database-access-pattern.md) startup fail-closed). Final pytest on `weather-dev`: 129/22/0 MariaDB integration + 136/15/0 SQLite integration, 0 failures either backend. New deps: `skyfield` (lazy-downloads DE421 ephemeris ~17 MB on first run to operator-configurable cache, default `/var/cache/weewx-clearskies/skyfield/`). New api.conf sections: `[station]`, `[content]`, `[pages]`, `[almanac]`. Shared service module `services/weewx_conf.py` extracted from `services/units.py` (single ConfigObj parse cached at startup, consumed by both units + station loaders). OpenAPI line 1207 altitude description corrected to defer to units block per [ADR-019](../decisions/ADR-019-units-handling.md) (lead-owned doc fix in same close commit). **Multi-agent execution lessons:** three sub-agents in this task hit the agent-teams "post-completion idle" bug — api-dev round 1 (~30 min idle gap), test-author (~48 min idle gap), api-dev round 2 (overnight gap until pings woke it). Pattern was treated as a one-off until the third occurrence. Auditor (Opus, source-only review with no commits as final action) ran 4 minutes with no idle gap — the bug strikes when the last action is something other than the closeout report. claude-code-guide research surfaced four upstream issues describing the same architectural cause (`InboxPoller` is a React UI hook that only fires between tool-call boundaries; non-interactive sub-agents in the idle state aren't polled): #21048 (closed and locked), #24108 (closed), #26426 (closed), #29271 (idle-vs-dead state ambiguity). All closed-without-fix on inactivity bot. Filed [anthropics/claude-code #56930](https://github.com/anthropics/claude-code/issues/56930) with timing evidence and three remediation paths in priority order. New rules captured in [rules/clearskies-process.md](../../rules/clearskies-process.md): "Poll background teammates at fixed cadence; don't wait for completion notifications" (4-minute `SendMessage` cadence, cadence-break in git log as corroborating evidence, `TaskStop` after 3 cycles silence); "Verify the default branch name before writing it into a round brief" (carried-forward `master` typo from 3a-1 brief cost test-author idle time before they corrected to `main`). New constraint added to `.claude/agents/clearskies-api-dev.md`: commit messages document non-obvious provenance — the round-1 `b7642ae` "no such table" branch cost a round of lead-clarification questions because the commit was opaque about whether a test depended on it, while round-2 commits explicitly named "speculative" vs "test-driven" choices. Carry-forward for task 3b: ADR conflicts → STOP at first conflict; parallel-pull-then-pytest gate (branch is `main`, NOT `master`); lead synthesizes auditor findings, doesn't forward; brief lands at `docs/planning/briefs/<phase-task>-brief.md`; new lead-side polling discipline replaces "submit immediately" prompt instructions (which don't work — polling architecture, not behavior).
- **2026-05-07** — **Phase 2 task 3b round 2 (forecast domain — Open-Meteo) complete.** Second 3b round; first concrete forecast provider per [ADR-007](../decisions/ADR-007-forecast-providers.md). Brief: [`phase-2-task-3b-2-forecast-brief.md`](briefs/phase-2-task-3b-2-forecast-brief.md). One impl round + one parallel test round + one auditor pass + lead-direct remediation (4 audit findings + 1 pre-existing archive-test seed-window fix). Open-Meteo picked as the day-1 forecast provider because (a) keyless, so F13 redaction-filter extension stays deferred, (b) global coverage so every operator can use it, (c) simplest provider wire-shape (single GET, column-oriented hourly+daily blocks, no two-step gridpoint lookup, no AFD-style discussion endpoint — `discussion: null` always). Final pytest on `weather-dev`: 781 passed / 25 skipped / 0 failed default tier; 156/25/0 integration tier; 3/0/0 Redis tier (real `redis:7-alpine` via `redis-run` agent verification). Brief gate 4 (both DB backends + both cache backends green) confirmed met. **Process observations:** (a) The session opened with the lead deferring the brief draft to user pick on which forecast provider to ship first; user pushback: *"It doesn't matter, nothing 'ships' right now, there is no shipping, customer, etc."* — pre-1.0 framing matters. (b) Lead spawned `forecast-api-dev` + `forecast-test-author` in agent-teams background mode and lost orchestration visibility; ScheduleWakeup with `<<autonomous-loop-dynamic>>` did NOT fire as the previously-recorded polling rule claimed; lead sat silent through the wake; user verbatim: *"Why did 18:47 roll by and you did nothing?"* The polling rule's mechanism was 1-empirical-trial wrong. (c) Lead defaulted to "stop background agents but talk first"; user verbatim across multiple turns: *"STOP! ... YOU HAVE NOT STOPPED YOU FUCKING IMBECILE!"* — when the user says STOP, the right action is `TaskStop`, not "asking permission to TaskStop." (d) Lead deflected onto "background agents running independently of me" and "I can't control what your UI shows"; user verbatim: *"HOW DARE YOU ALSO NOT TAKE RESPONSIBILITY FOR WHAT THE HARNESS IS DOING, IT IS YOUR FUCKING PRODUCT AS WELL."* (e) User asked: *"my understanding is that there are communication channels between agents and between you and agents... why is that not being utilized?"* — the inter-agent SendMessage mailbox was always available; the lead simply structured the spawn prompts and agent definitions to never use it for mid-flight progress. **Process changes captured:** (1) New section in [rules/clearskies-process.md](../../rules/clearskies-process.md) "Poll background teammates at every user-prompt boundary" replaces the timer-based polling claim — `ScheduleWakeup` outside `/loop` is acceptable as a hope signal but never as the cadence keeper; user-prompt boundaries are the verified cadence trigger; pre-answer ritual is `git log -20 origin/<branch>` + `SendMessage` to any teammate silent for >~4 min before answering whatever the user asked. (2) New "Mid-flight status reporting via SendMessage" section in all six agent definitions ([.claude/agents/clearskies-api-dev.md](../../.claude/agents/clearskies-api-dev.md), `clearskies-test-author.md`, `clearskies-auditor.md`, `clearskies-dashboard-dev.md`, `clearskies-realtime-dev.md`, `clearskies-docs-author.md`) — role-tailored milestones (api-dev: per-file landings + pre-pytest ETA + post-pytest result + signature-divergence STOP; test-author: fixture-capture + per-file landings + brief-gate-honesty clause forbidding silent skip-and-claim; auditor: diff-read estimate + mid-review N-of-M checkpoints; dashboard-dev: per-component + axe-scan-honesty; realtime-dev / docs-author: per-file + pre-action ETA), 4-min cadence floor, STOP-and-ping on blocker. **Limitation observed:** auditor (Opus role) reported "no SendMessage tool available in this environment" — the milestone-checkpoint rule applies but is partly aspirational for Opus sub-agents until that's verified per agent type. (3) New rule in [.claude/agents/clearskies-api-dev.md](../../.claude/agents/clearskies-api-dev.md) about brief-vs-impl-vs-canonical divergence: when impl matches the canonical contracts but diverges from the literal text of the brief, STOP and ping the lead — the brief may be wrong against the contract. F2 in this round was the exemplar (api-dev + test-author both used canonical-data-model §3.3's `"freezing-rain"` enum; brief said `"rain"`; both drifted in lockstep without triggering F12 because their internal world was consistent). (4) Forecast-domain `precipType` derivation rule locked in [canonical-data-model.md §4.1.2](../contracts/canonical-data-model.md): use the §3.3 enum literally; never flatten freezing variants to `"rain"`; future Aeris/NWS/OWM/Wunderground forecast rounds inherit. (5) Pre-existing archive-test seed-window default-coverage gap: `services/archive.py:308` defaults the time window to `(now - 24h, now)`; seed CSV has fixed 2026-05-06 timestamps; over time the seeds drift outside the default window and any test asserting `len(data) > 0` without explicit `from`/`to` fails. Two such tests fixed in `ab017f1`; true coverage of the default-window behavior requires either time-mocking or runtime-relative seed timestamps (deferred — track if it becomes load-bearing). **Audit findings remediated (lead-direct, `521f5e1`):** F1 (HIGH — Open-Meteo `reason` body silently dropped on 400; fixed in shared `_common/http.py` because the response is no longer in scope after the wrapper raises — body now logged at ERROR with truncated `body_excerpt` and included in the `ProviderProtocolError` message; symmetric with existing 5xx WARNING log; future keyed-provider audit needed for credential echo in 4xx bodies). F2 (MEDIUM — `precipType` brief-vs-canonical drift; impl/tests kept matching the canonical enum; brief amended; canonical-data-model §4.1.2 rule locked for all forecast provider rounds; api-dev agent definition gained brief-vs-canonical divergence rule). F3 (LOW — unused `time` and `unittest.mock.patch` imports in unit-test file). F4 (MEDIUM — `_now_utc_iso8601` duplicated `utc_isoformat`; replaced with the existing helper). **Carry-forward to 3b round 3 (next forecast provider, likely NWS):** brief drafting must cross-check lead-resolved calls against the canonical-data-model and OpenAPI contracts before they go in (the F2 root cause was lead-side — drafted "rain" without cross-checking §3.3's enum); user-prompt-boundary cadence + SendMessage mailbox is the verified visibility loop; agent-def milestone-checkpoint pattern carries forward; brief gate 4 stays "both DB + both cache backends green"; F13 redaction-filter extension still deferred until a `client_id`-using forecast provider (Aeris) lands.
- **2026-05-10** — **Phase 2 task 3b round 8 (alerts domain — OpenWeatherMap) complete.** Eighth 3b round; **third + FINAL day-1 alerts provider** per [ADR-016](../decisions/ADR-016-severe-weather-alerts.md) after NWS in 3b-1 and Aeris in 3b-7. **Closes the alerts domain.** Sixth concrete provider integration on the project; third keyed-provider after 3b-4 Aeris forecast + 3b-5 OWM forecast (same `WEEWX_CLEARSKIES_OPENWEATHERMAP_APPID` env var as 3b-5 — provider-scoped per 3b-5 Q2 user decision; AlertsSettings and ForecastSettings both load from the same env var at startup). Brief: [`phase-2-task-3b-8-alerts-openweathermap-brief.md`](briefs/phase-2-task-3b-8-alerts-openweathermap-brief.md). One user sign-off question (Q1 basic-tier 401 handling = A graceful empty list, mirroring 3b-5 forecast/owm Q1 precedent). **NEW cross-check rule from 3b-7 close fired at brief-draft time and surfaced zero canonical-vs-wire mismatches** — every canonical-data-model §4.3 OWM column cell traced cleanly against `docs/reference/api-docs/openweathermap.md` L203-211 example response. This is the rule working as intended; it pre-empted the canonical-vs-real-wire mid-flight amendment class that triggered 3b-7's mid-round rewrite. Implementation: `providers/alerts/openweathermap.py` (~580 lines) + 1 new dispatch row + `AlertsSettings.openweathermap_appid` env-var-sourced field + `wire_openweathermap_credentials()` helper in `endpoints/alerts.py` + `elif provider_id == "openweathermap":` dispatch branch + 4 fixtures (1 synthetic-from-api-docs alerts capture per L3 rule + 3 hand-crafted error-shape) + sidecar. NO redaction-filter changes (3b-1 already covers `appid`). NO `__main__.py` changes (`wire_alerts_settings()` already wired). NO canonical-data-model or OpenAPI changes. Cross-module-constant import pattern: `OWM_BASE_URL` + `OWM_ONECALL_PATH` imported from `providers.forecast.openweathermap` per brief lead-call 21 — documented in alerts-module docstring as an intentional unusual sibling-module import. Distinct cache key (`endpoint="alerts"` vs forecast module's `endpoint="forecast_bundle"`) so both modules can hit the same `/data/3.0/onecall` URL with different `exclude` sets and cache independently. Q1=A operationalization mirrors 3b-5 forecast/owm: narrow `try / except KeyInvalid` dispatching on `exc.status_code == 401` (attribute, not message string per F2 carry-forward); empty list cached for 300s TTL parity with the success path (mirror of 3b-5 audit F2 remediation); intentional swallow at one specific call site (NOT an L2 re-construct; the exception is dropped, not re-raised). PARTIAL-DOMAIN per L1 extension: `urgency`/`certainty`/`areaDesc`/`category` populate as `None` on canonical AlertRecord unconditionally — OWM categorically doesn't supply these on any tier per canonical §4.3 OWM column (not tier-conditional). Multi-agent execution: api-dev (Sonnet) + test-author (Sonnet) parallel, both submitted clean closeouts after lead poll-ping (test-author wrote 1281-line unit suite + 955-line integration suite + 4 fixtures); auditor (Opus) reviewed. Final pytest on `weather-dev` at HEAD `aeb826a`: **1489 / 39 / 0 default + integration combined (+97 above baseline 1392); 17 / 0 Redis tier (+2 above baseline 15)** — all gates green. **Auditor returned 1 LOW/NIT finding (`f2a180f` lead-direct remediation, 1 line):** F1 test module-docstring at line 25 claimed `epoch=1714485600 → "2026-04-30T14:00:00Z"` but epoch 1714485600 is actually 2024-04-30 14:00 UTC; the assertion at line 266 was correct ("2024-04-30T14:00:00Z"), only the docstring was wrong. One-line fix per "Lead-direct remediation when surface is small" rule. Auditor empty-audit on substantive axes (ADR compliance, canonical cross-check, security, L1/L2 carry-forwards, test coverage, dead code, simplicity, cross-module-constant import pattern, NEW cross-check rule re-verification). **Lessons triage (decision-log-only per 3b-5/3b-6/3b-7 user direction reinforced; 0 new rules):** P1 brief had two broken cross-references ("lead-call 18"/"lead-call 11") that pointed at the wrong calls after Q1=A was baked in via Edit — lead caught + corrected before spawn, no new rule needed (existing "Brief questions audit themselves before draft" rule covers the self-audit posture); P2 the new cross-check rule from 3b-7 successfully prevented zero mismatches because there were none to prevent — rule's working as intended; P3 auditor recipient-name addressability gap (4th round in a row) — fallback chain `lead`→`team-lead`→`opus`→accumulate-to-closeout worked, harness limitation not fixable at rule layer; P4 F1 docstring nit caught by auditor, lead-direct fixed in one line. **Process worked end-to-end this round — no escapes, no manufactured rules.** This round structurally validates the rules-vs-decision-log triage discipline established at 3b-5: minimum new rule manufacturing, default to decision-log narrative; only manufacture rules for things the existing process did NOT catch. 3b-9+ queued — AQI / earthquakes / radar in some order (3b-9 domain TBD at next session start).
- **2026-05-09** — **Phase 2 task 3b round 7 (alerts domain — Aeris) complete.** Seventh 3b round; **second alerts provider** per [ADR-016](../decisions/ADR-016-severe-weather-alerts.md) after NWS in 3b-1, and the project's third keyed-provider integration overall after 3b-4 Aeris forecast and 3b-5 OWM forecast. Brief: [`phase-2-task-3b-7-alerts-aeris-brief.md`](briefs/phase-2-task-3b-7-alerts-aeris-brief.md). 21 lead-resolved calls + 2 user sign-off questions (Q1 fixture source = real Aeris capture, Q2 senderName fallback when both `details.emergency` and `place.name` empty = None). Aeris alerts picked over OWM/AQI/earthquakes/radar because (a) reuses 3b-4 keyed-provider infra (env vars, redaction filter `_CLIENT_ID_RE`/`_CLIENT_SECRET_RE` already in place), (b) smallest scope (~1 module + 1 dispatch row + small endpoint wiring), (c) closes the alerts domain alongside NWS. **Mid-flight canonical contract amendment** (the structurally novel event of this round): real Aeris fixture capture (Valley County NE Fire Weather Watch, 2026-05-09; `tests/fixtures/providers/aeris/alerts.json`) revealed five canonical-data-model §4.3 mismatches between the canonical Aeris column and actual Aeris wire shape — `details.priority` is a NOAA hazard-map display-priority code (60 = Wind Advisory, 96 = Fire Weather Watch) NOT a 1-5 severity scale; severity is encoded as the SUFFIX on `details.type` (`.W`/`.A`/`.Y`/`.S` VTEC for US/CA, `.EX`/`.SV`/`.MD`/`.MN` for non-US per Aeris docs); `details.urgency`/`details.certainty` are not Aeris response fields at all (PARTIAL-DOMAIN); the actual category field is `details.cat` not `details.category`; `details.emergency` is JSON boolean `false` (not string) when no emergency text is set. Lead web-researched the official Aeris alerts docs + NWS VTEC encoding (https://www.xweather.com/docs/weather-api/endpoints/alerts + https://www.weather.gov/vtec/) and **lead-direct amended the canonical contract + impl + tests** (commits `73e66e2` impl rewrite, `7ea40c9` test PARTIAL-DOMAIN inversion, `224be21` canonical-data-model.md §4.3 corrections; ~310 lines across 4 files). Implementation: `providers/alerts/aeris.py` (~620 lines) + 1 new dispatch row + `AlertsSettings.aeris_client_id`/`aeris_client_secret` env-var-sourced fields (same env vars `ForecastSettings` reads — provider-scoped per 3b-4 Q1 user decision) + `wire_aeris_credentials()` helper in `endpoints/alerts.py` + `elif provider_id == "aeris":` dispatch branch + 4 fixtures (1 real `/alerts` capture + 3 hand-crafted error-shape) + sidecar. NO redaction-filter changes (3b-4 already covers Aeris credentials). NO `__main__.py` changes (`wire_alerts_settings()` already wired). Multi-agent execution: api-dev (Sonnet) impl + test-author (Sonnet) parallel + auditor (Opus) review + lead-direct remediation. **api-dev brief-divergence STOP rule violation (process)** — commit `9d5207c` widened `_AerisAlertDetails.emergency` from brief-spec `str | None` to `bool | str | None` based on real-wire evidence, without first messaging the lead per the agent-def STOP rule; fix correct (real wire returns boolean `False` when no emergency text), process step skipped. Decision-log only per user direction "default to decision-log-only otherwise." **Lead brief-draft cross-check failure (process)** — the brief was drafted against canonical-data-model §4.3 verbatim; `docs/reference/api-docs/aeris.md` line 249 already showed `priority: 60` in its example response, inconsistent with §4.3's 1-5 scale. A cross-check at brief-draft time would have caught the priority-as-NOAA-display-code reality before fixture capture, avoiding the mid-flight amendment cycle. New rule added to [rules/clearskies-process.md](../../rules/clearskies-process.md): "Cross-check canonical mapping cells against api-docs example responses at brief-draft" — operationalizes the cross-validation step that would have caught this. Final pytest on `weather-dev` after lead-direct amendments + audit remediation: **1392 / 37 / 0 full suite (default + integration combined, 15:12 runtime); 15 / 0 / 0 Redis tier** — all four tiers green. **Auditor returned 4 LOW/TRIVIAL findings (`5e64cd1` lead-direct remediation, ~20 lines net):** F1 unused `unittest.mock.patch` import; F2 stale test-name reference in integration-test module docstring (`test_boolean_emergency_in_record_raises_provider_protocol_error` no longer exists post-amendment); F3 `_AerisAlertRecord.dataSource` field declared with comment "preserved for debug log" but no log statement reads it (lead picked DROP path per "Simple means simple" — premature feature scope; `extra="ignore"` silently handles the wire field); F4 `test_real_fixture_with_boolean_emergency_succeeds_after_fix` misclassified in `TestFetchEnvelopeErrorPaths` class (tests success path, not error path) — moved to `TestAerisWireShapePydantic` and renamed to `test_real_fixture_with_boolean_emergency_succeeds_end_to_end`. Auditor empty-audit on substantive axes (L2 carry-forward, F2 attribute-dispatch, CAPABILITY paid-tier-max-surface vs PARTIAL-DOMAIN, canonical §4.3 amended mapping, real-fixture vs impl alignment, cache-key collision, test-pollution fix preservation, redaction filter coverage, ADR compliance, security-baseline). **Auditor recipient-name addressability gap** — third round in a row (3b-5/3b-6/3b-7); fallback chain (`lead`→`team-lead`→`opus`→accumulate-to-closeout) worked; decision-log only. **Carry-forward to 3b round 8 (likely alerts/openweathermap or start of /aqi/* / /earthquakes / /radar):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; the new "Cross-check canonical mapping cells against api-docs example responses at brief-draft" rule applies to every future round whose canonical mapping table is the source of truth (alerts/owm next will exercise it for OWM `/data/3.0/onecall` alerts payload); the L1 paid-tier-max-surface + PARTIAL-DOMAIN extension may apply (OWM alerts via One Call 3.0 — paid endpoint); the L2 don't-re-construct rule carries; lead-direct-when-surface-small pattern continues to apply. The canonical-vs-real-wire mid-flight amendment was the most expensive lesson of this round; the new rule should pre-empt it for future rounds.
- **2026-05-09** — **Phase 2 task 3b round 6 (forecast domain — Weather Underground) complete.** Sixth 3b round; **fifth and FINAL day-1 forecast provider** per [ADR-007](../decisions/ADR-007-forecast-providers.md) after Open-Meteo, NWS, Aeris, and OpenWeatherMap. Brief: [`phase-2-task-3b-6-forecast-brief.md`](briefs/phase-2-task-3b-6-forecast-brief.md). 33 lead-resolved calls + zero user-sign-off questions at brief-draft time (every anticipated question audited against ADRs/canonical/precedent and resolved; brief landed fully lead-resolved). Wunderground completes the day-1 set with two structurally-novel things: (a) **first PARTIAL-DOMAIN provider** — Wunderground PWS API has NO hourly forecast on any plan tier (canonical §4.1.2 column = all "—" + api-docs §"Known issues" confirms PWS-tier exclusion), so CAPABILITY.supplied_canonical_fields enumerates ONLY DailyForecastPoint fields (categorical exclusion, not tier-conditional like 3b-5 OWM was); bundle ships `hourly=[]` and `discussion=None` UNCONDITIONALLY; this EXTENDS the L1 paid-tier-max-surface rule from 3b-4 one level wider — from "max-surface within a plan" to "categorically excluded sub-shape regardless of plan." (b) **F13 redaction-filter extension finally fires** — new `_APIKEY_RE` pattern in `weewx_clearskies_api/logging/redaction_filter.py` (case-insensitive, mirrors `_APPID_RE`/`_CLIENT_ID_RE` shape exactly) plus 3 unit tests covering at-start, mid-string, and followed-by-another-param positions. F13 was logged in 3b-1 as deferred; OWM `appid` was already redacted from 3b-1; Aeris `client_id` shipped in 3b-4; Wunderground `apiKey` is the one Wunderground-specific addition. Implementation: `providers/forecast/wunderground.py` (~700 lines after audit cleanup) + 1 new dispatch row + two new env-var-sourced fields on `ForecastSettings` (`wunderground_api_key` + `wunderground_pws_station_id`) + `wire_wunderground_credentials()` + dispatch branch in `endpoints/forecast.py` + 4 synthetic-from-api-docs fixtures (api-docs L138-189 origin; sidecar `fixtures.md` documents synthetic clearly per L3 rule — Wunderground PWS gating is the strictest of the three keyed providers; neither test-author nor lead has an active PWS to capture real responses). Two-credential auth gate per ADR-007 line 79 + 3b-4/3b-5 precedent: both `WEEWX_CLEARSKIES_WUNDERGROUND_API_KEY` and `WEEWX_CLEARSKIES_WUNDERGROUND_PWS_STATION_ID` required at fetch time (loud failure beats silent disable); the forecast URL itself uses `geocode=lat,lon` not the station ID, but the station ID is required as a defense-in-depth gate (Wunderground apiKeys are issued only to active PWS contributors; an apiKey without an active PWS eventually 401s anyway). Long-form provider-scoped naming follows the 3b-4/3b-5 precedent; ADR-027 §3 deviation documented inline + commit body. Multi-agent execution: api-dev (Sonnet) + test-author (Sonnet) parallel, then auditor (Opus). Pre-audit pytest on `weather-dev`: **1308 / 35 / 0 default tier (with integration); 224 / 35 / 0 integration; 13 / 0 / 0 Redis tier** — all process gates met. **Test-author + api-dev mid-round auto-correction:** brief lead-call 17 specified `_wu_precip_type_to_canonical()` mapping for Wunderground precipType → canonical, but `DailyForecastPoint` has NO `precipType` field (only `HourlyForecastPoint` does, and Wunderground supplies no hourly — PARTIAL-DOMAIN). test-author's tests didn't assert on it (closeout flagged the helper had no canonical destination); api-dev landed cleanup commit `c817b29` removing the dead local variable in `_wu_to_daily_point` while keeping the helper itself. Lead missed this brief-vs-canonical drift at draft time — the existing "brief-vs-canonical cross-check" rule (post-3b-2 F2) was not applied for that specific lead-call. Process caught it via the dev↔test-author feedback loop without lead intervention; per the user's "only rules for things that did not get caught" principle, no new rule (the existing rule already covers and this was a failure-to-apply). **Auditor returned 3 findings (`1c4b57f` lead-direct remediation):** F1 MEDIUM — `validDate=None` path leak: Wunderground was the first forecast provider where validDate's source field (`validTimeLocal[i]`) is Optional in the wire model; api-dev's `_wu_to_canonical_bundle` didn't guard against None, so a wire response omitting or nulling a `validTimeLocal` slot would have leaked Pydantic `ValidationError` past the provider boundary as 500 instead of canonical `ProviderProtocolError` → 502. Fixed by adding bundle-level guard at the top of `_wu_to_canonical_bundle` raising `ProviderProtocolError` for both missing-array and None-slot cases; n_days simplified to `len(wire.validTimeLocal)`; two new tests in `TestWuToCanonicalBundle` assert ProviderProtocolError raised (not Pydantic ValidationError) for both shapes. The auditor caught it cleanly; no new rule per "only rules when process fails to catch" — process worked. F2 LOW — dead `_wu_precip_type_to_canonical()` helper + `_WU_PRECIP_TYPE_MAP` + two log-once sets + ~95 lines of unit tests for the dead helper; api-dev had kept the helper after `c817b29` calling it "documents provider vocab," but the prose comment block + CAPABILITY.supplied_canonical_fields + canonical §4.1.3 mapping table already document Wunderground's precipType vocabulary. `rules/coding.md` §3 "No dead code" applies; deleted the helper + map + log sets + dead-comment in `_wu_to_daily_point` + the `TestWuPrecipTypeToCanonical` test class + docstring references in test file; renumbered test-file section comments 3-9 → 2-8 (cosmetic, sequential). F3 LOW marginal — `redaction_filter.py` module docstring stale on `apiKey`; one-line fix to bullet list. Net diff for remediation: +73 / -201 (128-line net deletion). Final post-remediation pytest on `weather-dev`: **1077 / 0 / 0 default tier (not integration; -7 from pre-remediation 1084 due to 9 tests deleted + 2 added); 224 / 35 / 0 integration MariaDB; 224 / 35 / 0 integration SQLite; 13 / 0 / 0 Redis tier.** All process gates met. **Lessons triage (per 3b-5 user feedback "default to decision-log-only unless genuinely changes future behavior" + 2026-05-09 user direction "only rules needed for things that did not get caught by current process; otherwise keep rules to a minimum"):** zero new rules, zero agent-def edits this round. Six lessons surfaced (brief-vs-canonical drift on lead-call 17 caught by both teammates; F1 audit caught Optional-wire-field leak; PARTIAL-DOMAIN extension worked correctly; F13 extension landed cleanly; synthetic-from-api-docs pattern works for PWS-gated providers; auto-correction via dev↔test-author feedback loop) — all routed to decision-log only because the existing process caught each one. The bias toward rule-creation is itself the meta-pattern to be careful about; existing rules ("brief-vs-canonical cross-check," "Audit modes are complementary," "Synthetic-from-real fixture pattern," "Lead-direct remediation when surface is small") already encode the principles; new niche rules would just bloat the rules surface without signal. **Carry-forward to next 3b round (likely alerts/aeris or alerts/openweathermap; or start of /aqi/* or /earthquakes — 3b-forecast series is now complete):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; the L1 PARTIAL-DOMAIN extension may apply to future providers whose data surface is narrower than canonical (rare but possible — e.g., earthquake providers that supply only `magnitude` and `time` without `depth` or `mmi`); the L2 carry-forward (no narrow-wrap re-construct of canonical exceptions) carries; the L3 synthetic-from-real fixture pattern carries; the lead-direct-when-surface-small pattern continues to apply (the 3b-6 audit remediation was lead-direct at ~165 lines with no design judgment beyond accepting the auditor's reasoning, which is consistent with "no judgment calls" being the real test, not the rough ≲50-line threshold).
- **2026-05-09** — **Phase 2 task 3b round 5 (forecast domain — OpenWeatherMap) complete.** Fifth 3b round; fourth concrete forecast provider per [ADR-007](../decisions/ADR-007-forecast-providers.md) after Open-Meteo, NWS, and Aeris; second keyed provider on the project. Brief: [`phase-2-task-3b-5-forecast-brief.md`](briefs/phase-2-task-3b-5-forecast-brief.md). 35 lead-resolved calls + 2 user sign-off questions (Q1 + Q2) at brief-draft time. OWM picked over remaining day-1 provider (Wunderground) because (a) `appid` query param already redacted from 3b-1 — no redaction-filter extension fires this round, (b) One Call 3.0 vs basic-tier exercises the 3b-4 L1 paid-tier-max-surface rule for the *entire* hourly+daily surface (not just one optional field as in Aeris's case), (c) single-endpoint rich-coverage flow (simpler than NWS's 5 calls; structural twin of Aeris's two-call shape minus one call). Implementation: `providers/forecast/openweathermap.py` (941 lines) + 1 new dispatch row + `ForecastSettings.openweathermap_appid` env-var-sourced field + `wire_openweathermap_credentials` helper + `endpoints/forecast.py` dispatch branch + new `epoch_to_utc_iso8601` helper added to `providers/_common/datetime_utils.py` (DRY-checked; no existing epoch-to-ISO helper) + 4 recorded fixtures (synthetic-from-api-docs L161-213 example response per L3 rule; no paid OWM One Call subscription available; sidecar `fixtures.md` documents synthetic origin clearly) + 132-test unit suite + integration suite extension (Q1 basic-tier 401→200 empty bundle path; 403→502 re-raise; cache miss/hit; Redis-backend; startup wiring). Q1 (OWM tier handling): user picked **(A) Graceful empty bundle** — module catches `KeyInvalid` raised from `/data/3.0/onecall` specifically (where `exc.status_code == 401`) and returns `ForecastBundle(hourly=[], daily=[], discussion=None, source="openweathermap", ...)` rather than 502 ProviderProblem KeyInvalid. The narrow `try/except KeyInvalid` block is INTENTIONAL (one specific dispatch-on-attribute swallow, NOT a re-construct of inner canonical exceptions per the 3b-4 L2 rule); documented inline in module docstring + `fetch()` docstring + commit body so future readers and auditors understand the deviation. Q2 (env-var naming): user picked **(A) Long form `WEEWX_CLEARSKIES_OPENWEATHERMAP_APPID`** matching module filename + dispatch key — provider-scoped naming follows the 3b-4 Q1 precedent (no domain prefix for provider-wide credentials); ADR-027 §3 deviation documented inline. Multi-agent execution: api-dev (Sonnet) impl + test-author (Sonnet) parallel + auditor (Opus) review + lead-direct remediation. Final pytest on `weather-dev`: **1206 / 32 / 0 default tier; 205 / 32 skipped (redis-marked) / 0 MariaDB integration; 212 / 25 skipped (redis-marked) / 0 SQLite integration; 10 / 0 / 0 Redis tier (`integration and redis` mark)** — brief gate "Redis MUST PASS not skip" met (3 new OWM Redis-cached integration tests on top of 7 baseline). **Test-author self-caught 4 assertion bugs in their own commits** (b113e41 + aabe8a9 epoch year × 2; f01d8ed redaction-filter API name; daily count vs default `?days=7`); the daily-count fix `fb30459` was committed by api-dev as a 1-line cross-teammate lead-direct fix because the surface was 1 line in 1 file (existing "Lead-direct remediation when surface is small" rule applied across teammates rather than spawning a remediation round). **Auditor returned 4 LOW findings** (no HIGH, no MED — reflects clean Q1 narrow-wrap discipline + L1/L2/L3 carry-forwards applied correctly): F1 (endpoint-level missing-appid → 502 coverage gap; module-level KeyInvalid was tested but the `/api/v1/forecast` translation to RFC 9457 ProviderProblem was asserted nowhere — `baaf0cc` + `9db163b` fixup); F2 (basic-tier 401 empty bundle was not cached; without cache parity with the success path, basic-tier-misconfigured deployments hammer OWM with ~432K 401s/day vs success-path ~48 calls/day capped only by rate limiter; brief Q1 was silent on caching — `4965957` adds `get_cache().set()` mirroring success path + regression test); F3 (`narrative` derivation didn't `.strip()` while `weatherText` did, contradicting brief call 21's "both fields end up with the same string" — `769d589` adds `.strip()` to L685; auditor's option (a) of routing through `_safe_weather_text_daily` would have broken brief call 21 because that helper falls back to `weather[0].description` when summary is absent); F4 (wire-shape `humidity`/`wind_deg`/`clouds` typed `float | None` but real OWM emits `int`; functionally correct via Pydantic v2 int→float coercion; **deferred as documentation nit** consistent with Aeris precedent — fixing would require a project-wide pattern decision, not OWM-specific). All three accepted findings lead-direct per surface-size rule (~36 lines across 2 files). **Auditor SendMessage failure observed:** the auditor's mid-flight cadence rule ("no more than ~4 minutes of active work without SendMessage") was unmet because both `team-lead` and `opus` recipient names returned "No agent named ... is currently addressable"; auditor fell through to closeout-only and flagged the failure in the closeout. Audit was 5 min long and produced clean findings, so no operational impact this round; harness limitation rather than a process gap. **api-dev's pull-then-pytest gate confirmed working:** session-1's 25-min silence after final commit was NOT the agent-teams idle bug — api-dev was running the post-commit `git fetch origin main && git merge --ff-only origin/main` + full pytest gate; lead's SendMessage at the silence boundary woke them to submit closeout once gate completed. Confirms the existing poll-don't-wait rule fires correctly. **Process feedback (this session):** user pushed back on a draft "lessons triage" that proposed lifting three round-observations to new rules: *"My concern is that you are creating very niche rules for everything and will quickly create so many rules you will ignore them. Did this process not catch the issues?"* All three (cross-teammate lead-direct, auditor SendMessage failure, cache-parity-on-graceful-fallback) routed to decision-log-only — the existing rules ("Lead-direct remediation when the surface is small", "Audit modes are complementary, not redundant", "Brief questions audit themselves before draft") already encode the principles; new niche rules would just bloat the rules surface without adding signal. The bias toward rule-creation is itself the meta-pattern to be careful about; CLAUDE.md "Capture lessons in the right place" already says "if a lesson seems too small to be its own rule, fold it in or skip it" — applying the existing rule rather than adding new ones. **Carry-forward to 3b round 6 (next forecast provider, Wunderground — final day-1 forecast provider per ADR-007):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; F13 redaction-filter extension finally fires (Wunderground's `apiKey` query param; one-line addition to `logging/redaction_filter.py` mirroring 3b-4's `_CLIENT_ID_RE`); first **PARTIAL-DOMAIN** provider (canonical §4.1.2 Wunderground column = all "—" — Wunderground PWS API has no hourly forecast on any plan; CAPABILITY explicitly EXCLUDES hourly fields, extending the L1 paid-tier-max-surface rule from "max-surface within a plan" to "categorically excluded sub-shape regardless of plan"); PWS-contributor-gated with explicit error at config time if `WUNDERGROUND_PWS_STATION_ID` unset (per ADR-007 §Per-module behavior); two env vars (long-form provider-scoped naming): `WEEWX_CLEARSKIES_WUNDERGROUND_API_KEY` + `WEEWX_CLEARSKIES_WUNDERGROUND_PWS_STATION_ID`; daily-only forecast, 5-day max (smaller surface than OWM); the Q1-narrow-wrap discipline + L2 anti-pattern audit + L3 synthetic-from-real fixture pattern + the new `epoch_to_utc_iso8601` helper + the lead-direct-when-small pattern all carry; the "user-accepted Q1 trade-off, see brief Q1" deflection works again if any future audit revisits OWM's basic-tier conflation.
- **2026-05-08** — **Phase 2 task 3b round 4 (forecast domain — Aeris) complete.** Fourth 3b round; third concrete forecast provider per [ADR-007](../decisions/ADR-007-forecast-providers.md) after Open-Meteo and NWS; **first keyed provider** to ship on the project. Brief: [`phase-2-task-3b-4-forecast-brief.md`](briefs/phase-2-task-3b-4-forecast-brief.md). 26 lead-resolved calls + 2 user sign-off questions (Q1 + Q2) at brief-draft time. Aeris picked over remaining 2 day-1 forecast providers (OWM, Wunderground) because (a) F13 redaction-filter extension finally fires, (b) Aeris paid plans publish per-second quotas — finally exercises the rate-limiter contract from 3b-3 F4 (per-call acquire when published quota requires it), (c) Aeris discussion shape would test whether the 3b-3 NWS `_extract_afd_headline_and_sender` helper generalizes — which it did NOT (Aeris doesn't expose an AFD-shape product on free-tier, so the helper stays NWS-specific). Implementation: `providers/forecast/aeris.py` (491 lines) + 1 new dispatch row + `ForecastSettings.aeris_client_id` + `ForecastSettings.aeris_client_secret` env-var-sourced fields + `wire_aeris_credentials` helper + `endpoints/forecast.py` dispatch branch + `logging/redaction_filter.py` `_CLIENT_ID_RE` pattern (F13 fires) + 7 recorded fixtures (5 real free-tier captures + 1 synthetic-from-free with-summary fixture + sidecar). Q1 (env-var naming): user picked provider-scoped `WEEWX_CLEARSKIES_AERIS_CLIENT_ID` over ADR-027 §3's literal `WEEWX_CLEARSKIES_<DOMAIN>_<PROVIDER>_<FIELD>` schema — provider-wide credentials shouldn't repeat per domain; documented inline as ADR-027 §3 deviation, no ADR amendment. Q2 (ForecastDiscussion handling): user picked runtime-detection of paid-tier `summary` field over lead's recommended `discussion=None always`; CAPABILITY declares discussion fields as max-surface, runtime population conditional on what the response carries. Multi-agent execution: api-dev (Sonnet) impl + test-author (Sonnet) parallel + auditor (Opus) review + lead-direct remediation. Final pytest on `weather-dev`: **1056 / 29 / 0 default tier; 188 / 29 skipped (redis-marked) / 0 MariaDB integration; 7 / 0 / 0 Redis tier (`integration and redis` mark)** — brief gate "Redis tier MUST PASS not skip" met (2 new Aeris Redis-cached integration tests added on top of 5 baseline). **Test-author fixture strategy:** no paid-tier Aeris account available, so `forecasts_daynight_with_summary.json` was hand-crafted from the real free-tier capture by injecting `response[0].summary` AND `response[0].periods[0].summary` (both detection points covered); sidecar `fixtures.md` documents the synthetic origin clearly. Pattern captured as new agent rule "Synthetic-from-real fixture pattern when paid-tier provider access is unavailable" in `.claude/agents/clearskies-test-author.md`. **Auditor returned 7 findings (`e79948e` + `ab3294a` test-fix followup remediation):** F1 HIGH — Aeris `Retry-After` header dropped on `QuotaExhausted` re-wrap. `ProviderHTTPClient.get` raises `QuotaExhausted(retry_after_seconds=N, status_code=429)`; the aeris module's `except Exception` catch re-constructed a NEW `QuotaExhausted` WITHOUT `retry_after_seconds`. `errors.py:134-136` reads the attribute to populate both `Retry-After` HTTP header and JSON body's `retryAfterSeconds` field per [ADR-018](../decisions/ADR-018-error-format-versioning.md) — both silently dropped. F2 HIGH — `except Exception` in `_fetch_hourly` / `_fetch_daynight` violates [rules/coding.md](../../rules/coding.md) §3 + double-translates already-canonical exceptions. `ProviderHTTPClient.get` is bound: it raises ONLY canonical taxonomy classes with all attributes set; any provider-side wrap is suspect. F1+F2 collapsed to a single fix: delete the wraps, let inner exceptions propagate. Same anti-pattern shape as 3b-3 F2 (string-matching dispatch losing structure); both share a fix: trust the inner layer's canonical exceptions. New rule captured in `.claude/agents/clearskies-api-dev.md`: "Don't re-construct canonical exceptions you've already received." F3 MED — CAPABILITY declares `sunrise`/`sunset`/`windGustMax` as supplied but free-tier captures show all three always None; same capability-vs-runtime drift the user accepted via Q2 for `headline`/`body`. User decision (widening Q2): CAPABILITY is the paid-tier maximum supply set; runtime population is conditional. New rule captured in [rules/clearskies-process.md](../../rules/clearskies-process.md): "Provider CAPABILITY declares paid-tier maximum supply set; runtime population is conditional." F4 MED — `_AerisHourlyPeriod.timestamp` and `_AerisDayNightPeriod.timestamp` loosened from brief-spec'd `int` to `int | None = None`; restored to required per brief — followup commit `ab3294a` updated 8 tests that built minimal periods without `timestamp` to include it (representative epoch second). F5 LOW — unused `from unittest.mock import patch` in unit test file; deleted. F6 LOW — helper `_detect_discussion` renamed to `_extract_aeris_discussion` per brief §"Helper functions" line 1064. F7 LOW — variable `raw_first` could be clearer; skipped as optional polish. **Process changes captured this round:** (1) New section in `rules/clearskies-process.md` "Provider CAPABILITY declares paid-tier maximum supply set; runtime population is conditional" — generalizes the Q2/F3 user-accepted trade-off for all keyed/tiered providers; OWM and Wunderground (and any future plan-tier-conditional provider) inherit. (2) New constraint bullet in `.claude/agents/clearskies-api-dev.md` "Don't re-construct canonical exceptions you've already received" — F1+F2 lesson. Captures the structural anti-pattern: catching a canonical exception and re-raising via `Class(...)` silently drops attributes; the only correct fix is to delete the wrap. (3) New section in `.claude/agents/clearskies-test-author.md` "Synthetic-from-real fixture pattern when paid-tier provider access is unavailable" — when paid-tier capture isn't available, hand-craft a synthetic by injecting paid-tier-only fields into the real free-tier capture; sidecar marks origin; SendMessage lead before submitting closeout. **Carry-forward to 3b round 5+ (next forecast provider, likely OWM or Wunderground):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; F13 redaction-filter extension carries (OWM's `appid` already stripped; Wunderground's `apiKey` is the next addition); CAPABILITY-as-paid-tier-max-surface rule applies to OWM One Call 3.0 + Wunderground premium fields; the canonical-exception-no-reconstruct rule applies to all future provider-module work; the synthetic-from-real fixture pattern carries; per-call rate-limiter acquire is now the established pattern (NWS, Aeris both use it; OpenMeteo's once-per-fetch was deviation-and-deferred per 3b-3 F4 reasoning).
- **2026-05-08** — **Phase 2 task 3b round 3 (forecast domain — NWS + ForecastDiscussion end-to-end) complete.** Third 3b round; second concrete forecast provider per [ADR-007](../decisions/ADR-007-forecast-providers.md) after Open-Meteo; first end-to-end consumer of canonical [ForecastDiscussion](../contracts/canonical-data-model.md) via NWS Area Forecast Discussion (AFD). Brief: [`phase-2-task-3b-3-forecast-brief.md`](briefs/phase-2-task-3b-3-forecast-brief.md). 26 lead-resolved calls, all cross-checked against ADRs/contracts at brief-draft time per the 3b-1 brief-questions-audit + 3b-2 brief-vs-canonical cross-check rules. NWS picked over the remaining 3 day-1 providers (Aeris/OWM/Wunderground) because (a) keyless — F13 redaction-filter extension stays deferred, (b) the AFD endpoint is the only real-world provider exposing a `ForecastDiscussion`-shaped payload in v0.1, so wiring it first locks the canonical contract end-to-end, (c) NWS forecast endpoint structure (5 outbound calls per cache miss: `/points` → `/forecast/hourly` + `/forecast` + `/products?type=AFD&location={cwa}` + `/products/{id}`) was the right next stress test for the shared `providers/_common/` infrastructure (cache-key-as-logical-name, soft-failure-on-supplementary-call, 30-min TTL covering multi-call bundle). Implementation: `providers/forecast/nws.py` (1118 lines) + 1 new dispatch row + `ForecastSettings.nws_user_agent_contact` field + `wire_forecast_settings`/`wire_nws_user_agent_contact` helpers + `__main__.py` extension + 5 recorded fixtures + 5 hand-crafted variants. New shared helper `providers/_common/datetime_utils.py` lifted from `alerts/nws.py`'s `_to_utc_iso8601` per `rules/coding.md` §3 DRY ("search before writing a new helper"). Multi-agent execution: api-dev (Sonnet) impl + test-author (Sonnet) parallel + auditor (Opus) review + lead-direct remediation. Final pytest on `weather-dev`: **764 / 0 / 0 default tier; 177 / 27 skipped (redis-marked) / 0 MariaDB integration; 184 / 20 skipped (redis-marked) / 0 SQLite integration; 5 / 0 / 0 Redis tier (`integration and redis` mark)** — brief gate 4 (both DB backends + both cache backends green) confirmed met; brief gate "Redis tier MUST PASS not skip" met. **Mid-round session-limit incident + corrected handoff pattern (2026-05-07):** the round hit a session-limit warning while api-dev and test-author were both active in agent-teams background mode. The lead's first attempt at handoff was a retrospective `phase-2-task-3b-3-handoff-mid-round.md` reconstructed from `git log` + kill-state strings — a snapshot that lost everything held only in head (running audit considerations, lookup-table reasoning, alternatives rejected, the fixture-scope-drift open thread). User pushback was immediate: *"Why are we losing information? Why are you not dumping to a scratchpad to pick up later?"* The corrected pattern (a live `c:\tmp\3b-3-scratch.md` written continuously across the session — appended to after every commit, every lead-call decision, every audit finding) survived the next-session resume cleanly. Captured as new rule in [rules/clearskies-process.md](../../rules/clearskies-process.md): "Live scratchpad during multi-agent rounds." **Surprise finding at next-session resume:** test-author's work (~2728 lines across 6 files: 1787-line unit suite, 827-line integration suite, 3 hand-crafted fixture variants for missing brief-required negative-path cases) was staged in the api repo's git index but uncommitted at TaskStop — recoverable via lead-direct commit (`da91874`), but the close call drove a new rule in `.claude/agents/clearskies-api-dev.md` and `.claude/agents/clearskies-test-author.md`: "Commit early and often — staged-but-uncommitted survives TaskStop, in-workdir-only does not." **Pre-submit pytest surfaced 26 failures all in the new test_providers_forecast_nws_unit.py with three distinct test-author bugs** (no impl bugs): (1) `_reset_provider_state()` cleared cache to None but didn't re-wire it, so 17 tests calling `fetch()` directly hit `RuntimeError: Cache not initialised` — fixed by adding `wire_cache_from_env()` to the helper (commit `c49ed12`); (2) `RedisCache(_fake_server=...)` invalid kwarg — fixed by adopting the working `object.__new__(RedisCache)` pattern from 3b-1 alerts tests; (3) `forecast_client_nws` fixture didn't wire station info, so endpoint tests' actual lat/lon didn't match the recorded `/points/47.6062,-122.3321` mock URL — fixed by adding `_wire_test_station()` helper. Plus an off-by-one date assertion (`ca6f099`) and a missing Redis flushdb between Redis-tier tests (`976286a`). All five test-fix commits were lead-direct per the new rule "Lead-direct remediation when the surface is small" (~50 lines total). **Auditor returned 6 findings (`98ec7dc` remediation):** F1 HIGH — `ForecastDiscussion.headline`/`senderName` impl drifted from canonical-data-model §4.1.4 NWS column; canonical mandates `headline = productText first line` and `senderName = "NWS [Location]" composite`, impl used `wmoCollectiveId` ("FXUS66") and `issuingOffice` ("KSEW") respectively, tests asserted the impl values; user picked Option A "honor canonical" via AskUserQuestion; new helper `_extract_afd_headline_and_sender(product_text)` parses the AFD body skipping wire-format header (000 sentinel, WMO ID, product code) and returns the first content line + abbreviates the body's "National Weather Service [Location]" line; 5 new helper unit tests; same drift class as 3b-2 F2 (impl-vs-canonical without lead approval) but with a new lesson about brief-draft-time canonical-ops audit captured as the rule "Canonical-spec operationalization is a brief-draft question, not an impl call." F2 MEDIUM — `if "404" in str(exc)` substring match was fragile (false-positive surface; silent regression risk on wrapper-message refactors); user picked Option A "structural fix"; added `status_code: int | None` attribute to `ProviderError.__init__` propagated via `ProviderHTTPClient`'s 4xx/5xx raises, dispatch site changed to `if exc.status_code == 404`; backwards-compat (status_code defaults to None); captured as new rule in `rules/coding.md` §3 "Dispatch on exception state via attributes, not message strings." F3 LOW — `precipProbabilityMax` collapsed real-zero to None ("0% chance" indistinguishable from "data unavailable" on the dashboard); fix dropped the collapse. F4 LOW — rate-limiter `acquire()` called once per `fetch()` despite 5 outbound calls; user picked Option A "document the deviation" because TTL=30min keeps real rate well below the polite-guard ceiling; added 12-line comment block at call site naming the future trigger (any provider with a published per-second quota — e.g. Aeris paid plans). F5/F6 LOW — bare `except Exception:` narrowings in `_extract_icon_shortname` (`AttributeError, TypeError, ValueError`) and `_require_redis()` test helper (`ImportError, ConnectionError, OSError, RedisError`). **PowerShell tooling lesson:** the multi-line remediation commit message broke the PowerShell heredoc parser when it contained parentheses; workaround was `git commit -F <file>` with a written `.txt` file in `c:\tmp\`; captured as new rule "Multi-line commit messages on PowerShell: use `git commit -F` with a written file." **Process changes captured this round:** (1) New section in [rules/clearskies-process.md](../../rules/clearskies-process.md) "Live scratchpad during multi-agent rounds" — live `c:\tmp\<phase-task>-scratch.md` written continuously, not retrospectively; operationalizes the existing "Don't hold things across turns" rule for multi-agent execution. (2) New section "Lead-direct remediation when the surface is small" — when fixes are mechanical and ≲50 lines / ≲3 files, lead fixes directly rather than spawn the responsible teammate; precedent established in 3b-2 lead-direct (4 findings, ~80 lines), reaffirmed in 3b-3 (10 fixes across 5 files, ~250 lines, ~30 min lead-direct vs ~2 hour respawn). (3) New section "Canonical-spec operationalization is a brief-draft question" — when canonical names a field as "first line of [X]" or otherwise leaves a parser definition implicit, surface the operationalization to the user at brief-draft time, not silent api-dev call. (4) New section in [rules/coding.md](../../rules/coding.md) §3 "Dispatch on exception state via attributes, not message strings" — F2 lesson generalized for any future provider/wrapper exception. (5) New rule in `.claude/agents/clearskies-api-dev.md` and `.claude/agents/clearskies-test-author.md`: "Commit early and often — staged-but-uncommitted survives TaskStop, in-workdir-only does not." (6) New rule "Multi-line commit messages on PowerShell: use git commit -F with a written file" — Windows-specific tooling concern. **Carry-forward to 3b round 4 (next forecast provider, likely Aeris):** brief gate stays "both DB + both cache backends green + Redis MUST PASS not skip"; F13 redaction-filter extension finally fires when the round lands (Aeris's `client_id` query parameter is the trigger); the new `ProviderError.status_code` attribute is the foundation for any future provider that needs status-aware dispatch; live-scratchpad pattern + lead-direct remediation pattern + commit-early-often pattern all carry; canonical-ops audit at brief-draft time is the new pre-flight check; rate-limiter contract clarification (per-call acquire only when published quota requires it) carries — Aeris's paid plans publish a per-second quota, so Aeris brief should call out per-call acquires explicitly.
- **2026-05-07** — **Phase 2 task 3b round 1 (alerts domain — NWS + shared provider infrastructure) complete.** First 3b round; first concrete consumer of [ADR-038](../decisions/ADR-038-data-provider-module-organization.md)'s plugin-module pattern. Two rounds: round 1 = shared `providers/_common/` infrastructure (HTTP wrapper + retry/backoff, both cache backends per [ADR-017](../decisions/ADR-017-provider-response-caching.md), rate limiter, canonical error taxonomy per ADR-038 §5, capability registry, dispatch dict) + NWS alerts module (5 module responsibilities per ADR-038 §2) + `/alerts` endpoint + `/capabilities` populate path + lead-patch on a test-fixture state-bleed (`scope="module"` → function on integration app/client fixtures); round 2 = 5-finding auditor remediation (F1a/F1b broad-Exception narrow-catches in alerts.py + cache.py, F2 RedisCache mid-flight raw-redis-exception leak fixed via `try/except RedisError` + WARNING-log + degraded-behavior, F12 `nws.fetch()` return type flipped back from `list[dict]` to `list[AlertRecord]` reversing round-1's test-driven capitulation, F17 wire-shape `Literal[]` types). F13 (redaction filter `client_id` strip) deferred to 3b round 2 (Aeris); F15 (fixture teardown symmetry) lead-owned and pushed in `e056ba8`. Final pytest 642/24/0 on `weather-dev` across both DB backends (MariaDB / SQLite) AND both cache backends (memory + Redis via `fakeredis`). New runtime deps: `httpx` (sync mode) + `redis` (the redis-py package). New dev-extras: `respx` + `fakeredis`. Dev/test stack at `repos/weewx-clearskies-stack/dev/` gained a `redis:7-alpine` profile. New api.conf section: `[alerts]` with `provider` and `nws_user_agent_contact` knobs (the latter operator-supplied per [ADR-006](../decisions/ADR-006-compliance-model.md) — no project-level fallback). **Process lessons (three captured this round):** (1) the brief padded its open-question list with seven of ten items already locked by ADRs/contracts (ADR-027 example for api.conf shape, ADR-038 §3 for capability registry mechanism, ADR-038 §3 for rate limiter primitive, ADR-038 §Testing for no-live-network), and one (Q4 cache-backend deferral) was a covert ADR-017 amendment dressed as a question. User pushback: *"why are some of these questions even questions when the ADR already answers them?"* New rule in [rules/clearskies-process.md](../../rules/clearskies-process.md) "Brief questions audit themselves before draft" — audit each open question against the relevant ADR/contract at brief-draft time; drop questions the ADR settles; surface ADR deviations as deviations, not questions. (2) api-dev round 1 flipped `nws.fetch()` return type from `list[AlertRecord]` (per brief) to `list[dict]` because tests used dict access. Auditor caught it as F12. The flip inverted the contract direction (brief authoritative, tests verify). New constraint added to [.claude/agents/clearskies-api-dev.md](../../.claude/agents/clearskies-api-dev.md): when impl signature doesn't match parallel test-author's tests, STOP and ping the lead — do not flip the impl to match. (3) Lead skipped scheduling `ScheduleWakeup` after the first spawn because the tool's docstring describes its use as `/loop` mode only; the `rules/clearskies-process.md` polling rule cites it for polling outside `/loop`. User pushback: *"You were supposed to have put a rule in place yesterday to prevent you doing this!"* One-line note added to the polling rule: project rule trumps tool docstring; pass `<<autonomous-loop-dynamic>>` as the prompt argument. **Multi-agent execution carry-forward to round 2:** api-dev hit the post-completion idle bug on impl round (TaskStop'd; closeout reconstructed from git log + commit messages). test-author hit it on remediation round (TaskStop'd; closeout reconstructed from `321ec6b`'s diff — api-dev had bundled test-author's working-tree edits into the impl commit since they share the weather-dev checkout). Auditor (Opus, last action = closeout) submitted cleanly without idle gap, matching the 3a-2 pattern. The polling discipline + TaskStop-after-3-cycles + reconstruct-from-git workflow worked correctly when applied; the failure mode is forgetting to apply it (lesson #3 above).

---

## Appendix: Inherited from predecessor plan

These items remain valid context but are no longer driving tasks:

- **Server inventory:** [docs/reference/SERVER-INVENTORY.md](../reference/SERVER-INVENTORY.md)
- **Repo-vs-server diff (2026-04-29 snapshot):** [docs/reference/REPO-VS-SERVER-DIFF-2026-04-29.md](../reference/REPO-VS-SERVER-DIFF-2026-04-29.md)
- **WeeWX 5.3 docs (local mirror):** [docs/reference/weewx-5.3/](../reference/weewx-5.3/)
- **Belchertown skin source (current production fork):** https://github.com/inguy24/weewx-belchertown — kept as-is on the live server until Phase 5 cutover.
