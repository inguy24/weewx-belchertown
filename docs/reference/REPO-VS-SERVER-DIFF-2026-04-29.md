# REPO-VS-SERVER-DIFF — 2026-04-29

File-by-file comparison of the live skin on the **weewx container** (`/etc/weewx/skins/Belchertown/`) vs the three branches on your fork (https://github.com/inguy24/weewx-belchertown).

Generated by sha256-hashing every server file, then `git show <branch>:<path>` for each branch and comparing.

## Branches

- **`master`** — upstream `poblabs/weewx-belchertown` (vanilla)
- **`dropdowns`** — upstream + your customizations (chart sampling dropdown, custom pages, branding). 7 commits ahead, 12 files changed, +2899 lines.
- **`inguy24-changes`** — `dropdowns` + extra "Add files via upload" commits that put files at wrong paths. **Likely garbage from the GitHub web UI; do not merge.**

## File-by-file result

| File (under `skins/Belchertown/`) | matches `master` | matches `dropdowns` | matches `inguy24-changes` |
|---|---|---|---|
| `NOAA/NOAA-YYYY-MM.txt.tmpl` | MATCH | MATCH | MATCH |
| `NOAA/NOAA-YYYY.txt.tmpl` | MATCH | MATCH | MATCH |
| `about.inc` | differs | **differs** ⚠️ | differs |
| `about/index.html.tmpl` | MATCH | MATCH | MATCH |
| `belchertown-dark.min.css` | MATCH | MATCH | MATCH |
| `celestial.inc` | MATCH | MATCH | MATCH |
| `footer.html.tmpl` | MATCH | MATCH | MATCH |
| `graphs.conf` | differs | **differs** ⚠️ | differs |
| `graphs/index.html.tmpl` | MATCH | MATCH | MATCH |
| `header.html.tmpl` | differs | MATCH | MATCH |
| `images/aeris-icon-list.json` | MATCH | MATCH | MATCH |
| `index.html.tmpl` | MATCH | MATCH | MATCH |
| `index_radar.inc` | differs | MATCH | MATCH |
| `js/belchertown.js.tmpl` | differs | **differs** ⚠️ | differs |
| `js/responsive-menu.js` | MATCH | MATCH | differs (path moved in inguy24) |
| `json/weewx_data.json.tmpl` | MATCH | MATCH | MATCH |
| `kiosk.css` | MATCH | MATCH | MATCH |
| `kiosk.html.tmpl` | MATCH | MATCH | MATCH |
| `lang/ca.conf` | MATCH | MATCH | MATCH |
| `lang/de.conf` | differs | **differs** ⚠️ | differs |
| `lang/it.conf` | MATCH | MATCH | MATCH |
| `legal.inc` | differs | MATCH | MATCH |
| `legal/index.html.tmpl` | differs | MATCH | MATCH |
| `manifest.json.tmpl` | MATCH | MATCH | MATCH |
| `marine.inc` | differs | MATCH | MATCH |
| `marine/index.html.tmpl` | differs | MATCH | MATCH |
| `page-header.inc` | differs | MATCH | MATCH |
| `pi/index.html.tmpl` | MATCH | MATCH | MATCH |
| `records/index.html.tmpl` | MATCH | MATCH | MATCH |
| `reports/index.html.tmpl` | differs | **differs** ⚠️ | differs |
| `skin.conf` | differs | **differs** ⚠️ | differs |
| `style.css` | differs | **differs** ⚠️ | differs |

## Summary

### ✅ Server matches `dropdowns` for these custom pages/components

The `dropdowns` branch is what was deployed for these:

- `header.html.tmpl`
- `index_radar.inc`
- `legal.inc`, `legal/index.html.tmpl`
- `marine.inc`, `marine/index.html.tmpl`
- `page-header.inc`

### ⚠️ Server has 7 files that exist in NO branch — uncommitted server-side drift

These are the priority files to review and bring into the repo:

| File | Likely contains |
|---|---|
| **`graphs.conf`** | Chart definitions — the chart-sampling-dropdown configuration data |
| **`js/belchertown.js.tmpl`** | Frontend JS, including the chart sampling dropdown logic |
| **`style.css`** | Custom CSS for layout and the dropdown UI |
| **`skin.conf`** | Skin-level config (Belchertown options, possibly with secrets) |
| **`about.inc`** | About-page content |
| **`reports/index.html.tmpl`** | Reports page template |
| **`lang/de.conf`** | German translations |

These are exactly the files where field customization typically happens. The `dropdowns` branch represents an **earlier** state of the work; the server has been further modified since.

### Python backend (not in the table above — separate path)

- Server: `/etc/weewx/bin/user/belchertown.py` (252199 bytes, modified 2025-06-19)
- Server: `/etc/weewx/bin/user/belchertown.py.bak` (254775 bytes — proof of hand-editing)
- The `.bak` is *larger* than the live file, so someone deleted code from `belchertown.py` and saved a backup of the previous version.
- These need a dedicated 3-way comparison vs `master`, `dropdowns`, and `inguy24-changes`.

### `inguy24-changes` is broken

This branch:
- Has `belchertown.py` at the **top level** (5007 lines) AND at `bin/user/belchertown.py` — duplicate
- Has `js/` at the **top level** instead of `skins/Belchertown/js/` — wrong path
- Was built from "Add files via upload" commits (web UI uploads, easy to get wrong)

Recommendation: **leave it alone, don't merge it**. Treat it as a backup snapshot. If a file there matches the server and `dropdowns` doesn't, capture the content but don't pull in the branch.

## What to do next (NO CODING — proposal only)

The next phase is **capture** — bring the server's drift into a feature branch on the repo:

1. Pull each of the 7 drift files from the server into a feature branch (e.g., `feature/capture-server-drift-2026-04-29`).
2. Pull `bin/user/belchertown.py` and `belchertown.py.bak` into the same branch.
3. Diff each against `dropdowns` to understand WHAT changed (not just THAT it changed).
4. Strip any embedded secrets in `skin.conf` before committing (if the live file references API keys, externalize them or redact).
5. Open a PR from this feature branch describing each drift and its likely cause.
6. **Do not yet decide** whether to keep or refactor — capture first, evaluate second.

After capture is done, we'll have a complete picture and can answer:
- Is the chart-sampling dropdown salvageable as-is, or should it be redone?
- Is Belchertown still the right skin, or should we evaluate alternatives?
- What's needed to fix the MQTT typo and verify the wss chain end-to-end?
